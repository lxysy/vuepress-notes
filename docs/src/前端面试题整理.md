# 前端面试题整理（含答案）

本文档整理了金山云外包、盛天网络和东高科技、武汉中地云申、达昌技术、百安居、万云科技等公司的前端面试题及详细回答。

---

## 一、金山云外包一面

### 1. 防抖和节流

**防抖（Debounce）**：
- 在事件被触发 n 秒后再执行回调，如果在这 n 秒内又被触发，则重新计时
- 适用场景：搜索框输入、窗口 resize、表单验证
- 实现：

```javascript
function debounce(fn, delay) {
  let timer = null;
  return function (...args) {
    if (timer) clearTimeout(timer);
    timer = setTimeout(() => {
      fn.apply(this, args);
    }, delay);
  };
}
```

**节流（Throttle）**：
- 规定在一个单位时间内，只能触发一次函数。如果这个单位时间内触发多次函数，只有一次生效
- 适用场景：滚动加载、按钮点击、鼠标移动
- 实现（时间戳版）：

```javascript
function throttle(fn, interval) {
  let lastTime = 0;
  return function (...args) {
    const now = Date.now();
    if (now - lastTime >= interval) {
      lastTime = now;
      fn.apply(this, args);
    }
  };
}
```

**区别**：
- 防抖：事件停止触发后一段时间才执行（重新计时）
- 节流：事件持续触发时，每隔一段时间执行一次（有节奏地执行）

---

### 2. 用递归实现阶乘（如传入5，返回5*4*3*2*1）

```javascript
function factorial(n) {
  // 基准条件
  if (n <= 1) return 1;
  // 递归调用
  return n * factorial(n - 1);
}

console.log(factorial(5)); // 120
```

**尾递归优化版**：

```javascript
function factorial(n, result = 1) {
  if (n <= 1) return result;
  return factorial(n - 1, n * result);
}
```

---

### 3. 箭头函数和普通函数的区别

| 特性 | 箭头函数 | 普通函数 |
|------|---------|---------|
| `this` 指向 | 继承外层作用域的 `this`，不可改变 | 调用时确定，可通过 call/apply/bind 改变 |
| `arguments` | 没有自己的 `arguments`，可用 rest 参数 | 有自己的 `arguments` 对象 |
| 构造函数 | 不能作为构造函数，不能用 `new` | 可以作为构造函数 |
| `prototype` | 没有 `prototype` 属性 | 有 `prototype` 属性 |
| 返回值 | 单条语句可隐式返回 | 需要显式 return |
| 适用场景 | 回调函数、数组方法 | 对象方法、构造函数 |

**注意**：箭头函数不适合定义对象方法（因为 `this` 不指向对象本身）

---

### 4. HTTPS 和 HTTP 的区别

| 特性 | HTTP | HTTPS |
|------|------|-------|
| 安全性 | 明文传输，不安全 | SSL/TLS 加密传输，安全 |
| 默认端口 | 80 | 443 |
| 证书 | 不需要 | 需要 SSL 证书（CA 颁发） |
| 性能 | 相对较快 | 需要加密解密，开销稍大 |
| SEO | 无优势 | 搜索引擎优先收录 |

**HTTPS 加密过程**：
1. 客户端向服务器发送 HTTPS 请求
2. 服务器返回公钥证书
3. 客户端验证证书合法性
4. 客户端生成对称密钥，用公钥加密后发送给服务器
5. 服务器用私钥解密，获得对称密钥
6. 双方使用对称密钥加密通信

---

### 5. 前端缓存

前端缓存分为**强缓存**和**协商缓存**：

**强缓存**（不发请求到服务器）：
- `Cache-Control: max-age=xxx`（HTTP/1.1，单位秒）
- `Expires: 具体时间`（HTTP/1.0，绝对时间）
- 状态码：`200 (from memory cache)` 或 `200 (from disk cache)`

**协商缓存**（发请求到服务器确认）：
- `Last-Modified / If-Modified-Since`（基于文件修改时间）
- `ETag / If-None-Match`（基于文件内容哈希，优先级更高）
- 状态码：`304 Not Modified`

**缓存优先级**：
Service Worker > Memory Cache > Disk Cache > Push Cache > 网络请求

---

#### 【补充】前端缓存的实际应用场景与价值

**作为前端开发者，为什么要关心缓存？**

缓存是前端性能优化中最直接、最有效的手段之一。合理配置缓存可以减少网络请求、降低服务器压力、提升页面加载速度和用户体验。前端开发者需要与后端/运维协作，但更需要理解原理以便排查问题和提出优化方案。

---

**1. 强缓存的实际应用场景**

**场景一：静态资源长期缓存（JS/CSS/图片/字体）**

前端构建工具（Webpack/Vite）打包后的资源文件名包含哈希值（如 `main.a2f1b3c.js`），文件内容变化时哈希变化，URL 也变化。这种资源可以设置长期强缓存：

```nginx
# Nginx 配置示例
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    expires 1y;                    # 缓存一年
    add_header Cache-Control "public, immutable";
}
```

**效果**：用户首次访问后，这些资源在一年内直接从本地磁盘缓存读取，**零网络请求**，页面秒开。

**前端工作**：
- 确保构建产物启用 `[contenthash]` 或 `[hash]` 命名
- 在 HTML 中引用带哈希的资源（通过构建工具自动注入）

---

**场景二：第三方库 CDN 缓存**

使用 CDN 加载 Vue/React/jQuery 等库时，利用 CDN 的缓存分发能力：

```html
<script src="https://cdn.jsdelivr.net/npm/vue@3.2.0/dist/vue.global.js"></script>
```

CDN 节点已对这些资源做了强缓存，用户可能从距离最近的节点**毫秒级**获取。

**前端工作**：
- 评估是否使用 CDN（公共库适合，业务代码不适合）
- 配置 `integrity` 属性防止 CDN 篡改：

```html
<script src="https://cdn.example.com/lib.js" 
        integrity="sha384-xxx..." 
        crossorigin="anonymous"></script>
```

---

**2. 协商缓存的实际应用场景**

**场景三：HTML 入口文件使用协商缓存**

HTML 文件不能做强缓存（否则无法更新引用的 JS/CSS），但需要避免重复下载未修改的内容：

```nginx
# Nginx 配置
location /index.html {
    expires -1;                    # 禁用强缓存
    add_header Cache-Control "no-cache";  # 强制协商缓存
}
```

**工作流程**：
1. 用户每次访问，浏览器都请求 HTML
2. 服务端比对 `ETag` 或 `Last-Modified`
3. 如果 HTML 未修改 → 返回 `304 Not Modified`，浏览器使用缓存（仅消耗几百字节）
4. 如果 HTML 修改了（如引用了新版本的 JS）→ 返回 `200` 和新内容

**前端价值**：
- 保证用户总能获取最新资源引用关系
- 避免全量下载 HTML（通常几 KB 到几十 KB）
- 是**现代化构建（hash 文件名）+ 协商缓存 HTML** 的标准实践

---

**场景四：API 接口缓存**

不常变化的数据接口（如配置信息、字典数据、商品分类）可设置短时间强缓存 + 协商缓存：

```javascript
// 前端请求时主动控制缓存（axios 示例）
axios.get('/api/config', {
  headers: {
    'Cache-Control': 'max-age=3600'  // 建议浏览器缓存1小时
  }
});
```

或者由后端返回缓存头：

```http
HTTP/1.1 200 OK
Cache-Control: public, max-age=3600
ETag: "config-v2"
```

**前端工作**：
- 与后端约定哪些接口可缓存、缓存多久
- 对敏感数据（用户信息、订单）设置 `Cache-Control: no-store`

---

**3. 缓存策略在实际项目中的典型配置**

**现代化单页应用（SPA）的推荐缓存策略**：

| 资源类型 | 缓存策略 | 原因 |
|---------|---------|------|
| `index.html` | 协商缓存（no-cache） | 必须获取最新资源引用 |
| `*.js` / `*.css`（带 hash） | 强缓存 1年（immutable） | 内容变化 URL 变，可永久缓存 |
| 图片/字体/视频 | 强缓存 1年 | 不常变化，体积大 |
| API 数据（静态） | 强缓存 1小时 + ETag | 减少重复请求 |
| API 数据（动态） | 协商缓存或不缓存 | 保证数据实时性 |

---

**4. 前端开发中的缓存问题排查**

**常见问题 1：更新了代码，用户还是看到旧页面**

- **原因**：HTML 或 JS 被强缓存
- **排查**：打开 Network 面板，看状态码是 `200 (from disk cache)` 还是 `304`
- **解决**：
  - 确保 HTML 使用协商缓存而非强缓存
  - 确保 JS/CSS 文件名包含 hash
  - 紧急修复时修改 HTML 的查询参数强制刷新：`<script src="app.js?v=2">`

**常见问题 2：调试时缓存干扰开发**

- **解决**：
  - Chrome DevTools → Network → 勾选 `Disable cache`
  - 按 `Ctrl+Shift+R`（Windows）或 `Cmd+Shift+R`（Mac）强制刷新
  - 在 URL 后加随机数：`?t=${Date.now()}`

**常见问题 3：缓存优先级导致 Service Worker 缓存不更新**

- **原因**：Service Worker 拦截请求后，可能优先返回旧缓存
- **排查**：Application → Service Workers → 查看缓存内容
- **解决**：更新 SW 脚本，在 `activate` 阶段清理旧缓存（见 Service Worker 章节）

---

**5. 前端可以主动控制的缓存**

除了依赖 HTTP 缓存头，前端还可以通过以下方式管理缓存：

**LocalStorage / SessionStorage**：

```javascript
// 缓存不常变化的配置数据（注意 5MB 限制）
const config = localStorage.getItem('app_config');
if (config) {
  return JSON.parse(config);  // 使用本地缓存
}

const freshConfig = await fetch('/api/config').then(r => r.json());
localStorage.setItem('app_config', JSON.stringify(freshConfig));
```

**IndexedDB**：

```javascript
// 缓存大量结构化数据（如离线可用的文档、图片）
const db = await openDB('AppCache', 1, {
  upgrade(db) {
    db.createObjectStore('documents', { keyPath: 'id' });
  }
});

// 先读缓存，再请求更新（Stale While Revalidate 的前端实现）
const cached = await db.get('documents', docId);
if (cached) render(cached);  // 立即显示

const fresh = await fetch(`/api/doc/${docId}`).then(r => r.json());
await db.put('documents', fresh);
render(fresh);  // 更新为最新
```

**Cache API（配合 Service Worker）**：

```javascript
// 在 Service Worker 中使用，精确控制缓存内容
caches.open('api-cache-v1').then(cache => {
  cache.add('/api/user/profile');      // 缓存请求
  cache.match('/api/user/profile');    // 读取缓存
  cache.delete('/api/user/profile');   // 删除缓存
});
```

---

**6. 缓存的价值总结**

| 指标 | 无缓存 | 合理缓存后 | 提升效果 |
|------|--------|-----------|---------|
| 页面二次加载时间 | 2-5 秒 | 0.5-1 秒 | **提升 70-80%** |
| 服务器带宽消耗 | 100% | 20-30% | **节省 70%+** |
| 用户流量消耗 | 高 | 低 | 移动端尤为重要 |
| 服务器 QPS 压力 | 高 | 低 | 支撑更多并发用户 |

**前端工程师的核心价值**：
1. 设计合理的构建产物命名策略（hash），让强缓存成为可能
2. 与后端协商 API 缓存策略，减少不必要的数据请求
3. 使用 Service Worker / IndexedDB 实现离线可用和秒开体验
4. 能够快速定位和解决用户反馈的"看到旧页面"问题

---

### 6. ES6 新增哪些特性

- **let / const**：块级作用域声明，不存在变量提升
- **箭头函数**：简洁语法，`this` 继承外层
- **模板字符串**：反引号 `` ` `` 包裹，支持 `${}` 插值
- **解构赋值**：数组/对象解构 `const {a, b} = obj`
- **展开运算符**：`...arr`，用于展开数组或对象
- **Promise**：异步编程解决方案，解决回调地狱
- **Class**：语法糖，更清晰的面向对象编程
- **模块化**：`import` / `export`
- **Map / Set**：新的数据结构
- **Symbol**：唯一值类型
- **Proxy / Reflect**：元编程支持
- **默认参数**：函数参数可设默认值
- **for...of**：遍历可迭代对象

---

### 7. Vuex

Vuex 是 Vue 的**状态管理库**，采用集中式存储管理应用的所有组件的状态。

**核心概念**：
- **State**：存储应用的状态数据
- **Getters**：基于 state 的计算属性
- **Mutations**：修改 state 的唯一途径（必须同步）
- **Actions**：提交 mutations，可包含异步操作
- **Modules**：将 store 分割成模块

**流程**：
`State → Vue Components → Actions → Mutations → State`

---

### 8. 原型和原型链

**原型（Prototype）**：
- 每个函数都有一个 `prototype` 属性，指向原型对象
- 原型对象默认有一个 `constructor` 属性指回构造函数
- 实例通过 `__proto__` 访问原型

**原型链（Prototype Chain）**：
- 当访问对象的属性时，先在自身查找，找不到就沿 `__proto__` 向上查找
- 直到找到 `Object.prototype`，其 `__proto__` 为 `null`
- 这条查找路径就是原型链

```javascript
// 示例
function Person() {}
const p = new Person();
p.__proto__ === Person.prototype; // true
Person.prototype.__proto__ === Object.prototype; // true
Object.prototype.__proto__ === null; // true
```

---

### 9. 异步任务分为哪些

JavaScript 的异步任务分为：

**宏任务（Macrotask）**：
- `setTimeout` / `setInterval`
- `setImmediate`（Node.js）
- I/O 操作
- UI 渲染
- `requestAnimationFrame`

**微任务（Microtask）**：
- `Promise.then` / `catch` / `finally`
- `MutationObserver`
- `queueMicrotask`
- `process.nextTick`（Node.js，优先级最高）

**执行顺序**：同步代码 → 微任务队列清空 → 宏任务 → 微任务队列清空 → 下一个宏任务...

---

#### 【补充】MutationObserver 与 queueMicrotask 详解

**1. MutationObserver**

**是什么**：
MutationObserver 是浏览器提供的 **DOM 变动观察器**，用于异步监听 DOM 树的变化（节点增删、属性修改、文本内容变化）。它是微任务的一种实现机制。

**核心特点**：
- **异步执行**：DOM 变化不会立即触发回调，而是等待当前宏任务执行完毕后，在微任务阶段批量处理
- **批量合并**：多次 DOM 变化会合并为一次回调（通过 `takeRecords` 获取变动列表）
- **性能友好**：相比已废弃的 `MutationEvent`，不会阻塞渲染线程

**常见使用场景**：

```javascript
// 场景 1：监听 DOM 变化执行后续逻辑（如自动滚动到底部）
const observer = new MutationObserver((mutations, obs) => {
  // mutations 是 DOM 变动数组
  mutations.forEach((mutation) => {
    if (mutation.type === 'childList') {
      console.log('子节点变化:', mutation.addedNodes, mutation.removedNodes);
    }
    if (mutation.type === 'attributes') {
      console.log('属性变化:', mutation.attributeName, mutation.target);
    }
  });
});

// 配置观察选项
observer.observe(document.getElementById('chat-box'), {
  childList: true,        // 监听子节点增删
  attributes: true,       // 监听属性变化
  characterData: true,    // 监听文本内容变化
  subtree: true,          // 监听后代节点（不仅直接子节点）
  attributeOldValue: true,// 记录属性旧值
  characterDataOldValue: true
});

// 停止观察
observer.disconnect();
```

**场景 2：作为微任务调度器（历史兼容方案）**

在 `queueMicrotask` 出现之前，MutationObserver 是实现微任务的常用技巧：

```javascript
// 旧版浏览器中模拟微任务（现在已不推荐，用 queueMicrotask 替代）
function microTask(callback) {
  const node = document.createTextNode('');
  const observer = new MutationObserver(() => {
    callback();
    observer.disconnect();
  });
  observer.observe(node, { characterData: true });
  node.data = 'trigger'; // 触发变化
}

// 效果：callback 在当前同步代码后、下一个宏任务前执行
microTask(() => console.log('微任务执行'));
console.log('同步代码');
// 输出：同步代码 → 微任务执行
```

**为什么 MutationObserver 是微任务？**

浏览器设计如此：DOM 变化频繁，如果每次变化都同步触发回调，会导致大量重排重绘，阻塞 UI。将其放入微任务队列，可以：
1. 等待当前同步代码执行完毕（避免中间状态被观察到）
2. 批量合并多次变动（一次回调处理所有变化）
3. 在 UI 渲染前完成（确保观察逻辑不滞后）

---

**2. queueMicrotask**

**是什么**：
`queueMicrotask` 是浏览器和 Node.js 提供的 **标准微任务调度 API**，用于将回调函数放入微任务队列，在当前同步代码执行完毕后立即执行。

**语法**：

```javascript
queueMicrotask(() => {
  console.log('这是一个微任务');
});
```

**与 setTimeout(..., 0) 的区别**：

```javascript
console.log('1. 同步代码开始');

setTimeout(() => {
  console.log('4. setTimeout 宏任务');
}, 0);

queueMicrotask(() => {
  console.log('3. queueMicrotask 微任务');
});

console.log('2. 同步代码结束');

// 输出顺序：1 → 2 → 3 → 4
```

| 特性 | `queueMicrotask` | `setTimeout(..., 0)` |
|------|-----------------|---------------------|
| **任务类型** | 微任务 | 宏任务 |
| **执行时机** | 当前同步代码后立即执行 | 等待所有微任务执行完毕后，下一个宏任务阶段 |
| **延迟** | 无延迟（当前事件循环末尾） | 最小 4ms（HTML5 规范限制） |
| **使用场景** | 需要尽快执行但不阻塞当前任务 | 需要让出主线程，允许 UI 更新 |

**实际应用场景**：

```javascript
// 场景 1：拆分大任务避免阻塞主线程（配合微任务实现伪异步）
function processLargeData(data) {
  const chunkSize = 100;
  let index = 0;
  
  function processChunk() {
    const chunk = data.slice(index, index + chunkSize);
    // 处理数据...
    
    index += chunkSize;
    if (index < data.length) {
      // 使用 queueMicrotask 而非 for 循环，让出执行权
      queueMicrotask(processChunk);
    }
  }
  
  processChunk();
}

// 场景 2：状态更新后异步执行副作用（确保 DOM 已更新）
class Component {
  update(data) {
    this.state = data;
    
    // 将副作用推入微任务，确保状态更新已完成
    queueMicrotask(() => {
      this.notifySubscribers();
      this.updateDOM();
    });
  }
}

// 场景 3：Promise 的 polyfill 实现
function MyPromise(executor) {
  // ...
  const resolve = (value) => {
    // 使用 queueMicrotask 保证 then 回调异步执行
    queueMicrotask(() => {
      this.value = value;
      this.onFulfilledCallbacks.forEach(cb => cb(this.value));
    });
  };
  executor(resolve, reject);
}
```

**与 Promise.then 的关系**：

```javascript
// 以下三者在微任务队列中优先级相同，按入队顺序执行
Promise.resolve().then(() => console.log('Promise'));
queueMicrotask(() => console.log('queueMicrotask'));

const observer = new MutationObserver(() => console.log('MutationObserver'));
observer.observe(document.body, { childList: true });
document.body.appendChild(document.createElement('div'));
observer.disconnect();

// 输出顺序（按入队顺序）：Promise → queueMicrotask → MutationObserver
```

**注意事项**：

```javascript
// 危险：在微任务中递归创建微任务可能阻塞渲染
function dangerous() {
  queueMicrotask(() => {
    console.log('微任务');
    dangerous(); // 递归创建，宏任务永远无法执行
  });
}
// 页面将卡死，setTimeout、用户点击等宏任务无法响应

// 安全做法：需要持续执行时，间歇性让出主线程
function safe() {
  queueMicrotask(() => {
    console.log('微任务');
    if (shouldContinue) {
      setTimeout(safe, 0); // 使用宏任务让出执行权
    }
  });
}
```

**总结**：

| API | 任务类型 | 主要用途 | 现代替代方案 |
|-----|---------|---------|------------|
| `Promise.then` | 微任务 | 异步流程控制、链式调用 | 原生支持，无需替代 |
| `MutationObserver` | 微任务 | DOM 变动监听、历史微任务模拟 | `queueMicrotask`（调度）/ `IntersectionObserver`（可见性） |
| `queueMicrotask` | 微任务 | 标准微任务调度、拆分同步逻辑 | 原生 API，最推荐 |
| `process.nextTick` | 微任务（Node.js） | Node.js 中最高优先级异步操作 | `queueMicrotask`（跨平台统一） |

理解这些微任务机制，对于分析事件循环输出、实现自定义 Promise、优化长任务拆分至关重要。

---

### 10. 前端性能优化问题

**加载优化**：
- 代码分割（Code Splitting）、懒加载（Lazy Load）
- 资源压缩（Gzip/Brotli）、Tree Shaking
- CDN 加速、HTTP/2、缓存策略
- 预加载（Preload）、预连接（Preconnect）

**渲染优化**：
- 减少重排重绘，使用 `transform` 和 `opacity`（开启 GPU 加速）
- 虚拟滚动（Virtual List）处理大数据列表
- 图片懒加载、使用 WebP 格式
- 使用 `requestAnimationFrame` 做动画

**运行时优化**：
- 防抖节流、事件委托
- Web Worker 处理复杂计算
- Service Worker 做离线缓存
- 减少内存泄漏

---

#### 【补充】懒加载、预加载、预连接详解

**1. 懒加载（Lazy Load）**

懒加载是一种延迟加载非关键资源的技术，只有当资源即将进入可视区域时才加载。

**适用场景**：
- 图片懒加载（长列表、瀑布流）
- 组件懒加载（路由懒加载、模态框）
- 脚本/样式延迟加载（非首屏必要资源）

**实现方式**：

```html
<!-- 1. 原生图片懒加载（Chrome 76+） -->
<img src="placeholder.jpg" data-src="real.jpg" loading="lazy" alt="">

<!-- 2. 路由懒加载（Vue/React） -->
// Vue
const Home = () => import('./views/Home.vue');

// React
const Home = lazy(() => import('./pages/Home'));
```

**IntersectionObserver 实现**：

```javascript
const imageObserver = new IntersectionObserver((entries, observer) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const img = entry.target;
      img.src = img.dataset.src;    // 加载真实图片
      img.classList.add('loaded');   // 添加过渡动画
      observer.unobserve(img);       // 停止观察
    }
  });
});

// 配置项：提前 200px 开始加载（预加载阈值）
const options = {
  root: null,           // 视口作为参照
  rootMargin: '200px',  // 提前 200px 触发
  threshold: 0          // 元素可见 0% 时触发
};

document.querySelectorAll('img[data-src]').forEach(img => {
  imageObserver.observe(img);
});
```

**骨架屏配合懒加载**：
- 先用占位图/骨架屏占据位置，避免布局抖动（CLS）
- 图片加载完成后淡入显示

---

**2. 预加载（Preload / Prefetch / Preconnect）**

浏览器提供的 `<link rel="...">` 资源提示机制，提前获取或准备后续需要的资源。

| 属性 | 作用 | 优先级 | 使用场景 |
|------|------|--------|---------|
| `preload` | 当前页面必定需要的资源 | 高 | 首屏关键字体、CSS、JS |
| `prefetch` | 下一页面可能需要的资源 | 低（空闲时加载） | 路由预加载、后续页面资源 |
| `preconnect` | 提前建立 TCP/TLS 连接 | 中 | 第三方域名（CDN、API）|
| `dns-prefetch` | 提前解析 DNS | 低 | 多域名资源预解析 |
| `prerender` | 后台渲染整个页面 | 最低 | 用户极可能访问的下一页 |

**代码示例**：

```html
<!-- 1. Preload：加载当前页面关键资源（会立即下载） -->
<link rel="preload" href="/fonts/main.woff2" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="/css/critical.css" as="style">
<link rel="preload" href="/js/main.js" as="script">

<!-- 2. Prefetch：空闲时预加载下一页资源 -->
<link rel="prefetch" href="/about.html">
<link rel="prefetch" href="/js/chunk-about.js">

<!-- 3. Preconnect：提前建立连接，减少握手时间（建议 2-6 个） -->
<link rel="preconnect" href="https://cdn.example.com">
<link rel="preconnect" href="https://api.example.com" crossorigin>

<!-- 4. DNS-Prefetch：仅解析 DNS，比 preconnect 更轻量 -->
<link rel="dns-prefetch" href="https://fonts.googleapis.com">

<!-- 5. Prerender：后台完整渲染页面（资源消耗大，谨慎使用） -->
<link rel="prerender" href="/next-page.html">
```

**动态预加载（JavaScript）**：

```javascript
// 根据用户行为预加载（如鼠标悬停在链接上）
document.querySelectorAll('a[data-prefetch]').forEach(link => {
  link.addEventListener('mouseenter', () => {
    const prefetchLink = document.createElement('link');
    prefetchLink.rel = 'prefetch';
    prefetchLink.href = link.href;
    document.head.appendChild(prefetchLink);
  });
});
```

**Webpack 中的预加载**：

```javascript
// 魔法注释实现预加载
const About = () => import(/* webpackPrefetch: true */ './About.vue');
const Admin = () => import(/* webpackPreload: true */ './Admin.vue');
```

**注意事项**：
- `preload` 资源必须在本页面使用，否则会浪费带宽并触发控制台警告
- `preload` 的 `as` 属性必须正确设置（font/script/style/image），否则可能不生效
- `preconnect` 过多反而会增加开销，建议控制在 6 个以内

---

**3. Web Worker**

Web Worker 允许在浏览器后台线程中运行 JavaScript，不阻塞主线程（UI 线程）。

**适用场景**：
- 大数据排序/过滤/聚合（Excel 数据处理）
- 图像处理（滤镜、压缩、格式转换）
- 复杂数学计算（加密解密、科学计算）
- 文本分析（代码高亮、Markdown 渲染）
- 实时数据解析（WebSocket 大数据流）

**Worker 类型**：

| 类型 | 特点 | 使用场景 |
|------|------|---------|
| **Dedicated Worker** | 专属页面，一对一通信 | 大部分场景 |
| **Shared Worker** | 多页面/iframe 共享 | 跨 Tab 数据同步 |
| **Service Worker** | 代理网络请求，可离线 | PWA、离线缓存 |

**基础用法**：

```javascript
// main.js（主线程）
const worker = new Worker('worker.js');

// 发送数据给 Worker（结构化克隆算法，可传大部分数据类型）
worker.postMessage({ type: 'SORT', data: largeArray });

// 接收 Worker 结果
worker.onmessage = (e) => {
  console.log('排序完成:', e.data.result);
  // 更新 UI
};

worker.onerror = (err) => {
  console.error('Worker 错误:', err.message);
};

// 不再需要时终止 Worker（释放内存）
worker.terminate();
```

```javascript
// worker.js（Worker 线程）
self.onmessage = (e) => {
  const { type, data } = e.data;
  
  if (type === 'SORT') {
    // 执行耗时操作（不会阻塞主线程）
    const sorted = data.sort((a, b) => a - b);
    
    // 返回结果
    self.postMessage({ result: sorted });
  }
};

// Worker 中可使用的 API：
// setTimeout/setInterval, fetch, XMLHttpRequest, IndexedDB, Cache API
// 不可使用：DOM, window, document, alert, localStorage（可间接通过主线程操作）
```

**Inline Worker（避免单独文件）**：

```javascript
// 使用 Blob URL 创建内联 Worker
const workerScript = `
  self.onmessage = (e) => {
    const result = e.data.map(x => x * 2);
    self.postMessage(result);
  };
`;

const blob = new Blob([workerScript], { type: 'application/javascript' });
const worker = new Worker(URL.createObjectURL(blob));
```

**Comlink 简化 Worker 通信**：

```javascript
// 使用 Comlink 库（Google 出品），像调用普通函数一样使用 Worker
import * as Comlink from 'comlink';

const worker = new Worker('./calc.worker.js');
const calc = Comlink.wrap(worker);

// 直接调用 Worker 中的函数
const result = await calc.fibonacci(40); // 不阻塞 UI
```

**性能建议**：
- Worker 创建有开销（约 10-50ms），可复用而非频繁创建/终止
- 大数据传递使用 **Transferable Objects**（零拷贝）

```javascript
// Transferable Objects：将数据所有权转移给 Worker，原上下文无法再访问
const buffer = new ArrayBuffer(1024 * 1024);
worker.postMessage({ buffer }, [buffer]);
// 此时主线程的 buffer 已被清空
```

---

**4. Service Worker**

Service Worker 是运行在浏览器背后的独立线程，可代理网络请求、管理缓存，是实现 PWA（渐进式 Web 应用）的核心技术。

**核心特性**：
- **离线访问**：缓存资源，无网络时也能访问
- **网络代理**：拦截请求，决定从缓存还是网络获取
- **后台同步**：网络恢复后自动发送失败请求
- **推送通知**：接收服务器推送消息
- **独立生命周期**：与页面无关，可后台运行

**生命周期**：

```
注册(register) → 安装(install) → 激活(activate) → 闲置/终止
                     ↓                    ↓
                  缓存静态资源          清理旧缓存
```

**基本用法**：

```javascript
// main.js：注册 Service Worker
if ('serviceWorker' in navigator) {
  window.addEventListener('load', () => {
    navigator.serviceWorker.register('/sw.js')
      .then(registration => {
        console.log('SW 注册成功:', registration.scope);
      })
      .catch(err => {
        console.log('SW 注册失败:', err);
      });
  });
}
```

```javascript
// sw.js：Service Worker 脚本
const CACHE_NAME = 'app-v1';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/css/main.css',
  '/js/main.js',
  '/icon.png'
];

// 安装阶段：缓存静态资源
self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then(cache => cache.addAll(STATIC_ASSETS))
      .then(() => self.skipWaiting()) // 立即激活新 SW
  );
});

// 激活阶段：清理旧缓存
self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then(cacheNames => {
      return Promise.all(
        cacheNames
          .filter(name => name !== CACHE_NAME)
          .map(name => caches.delete(name))
      );
    }).then(() => self.clients.claim()) // 立即控制所有页面
  );
});

// 拦截网络请求
self.addEventListener('fetch', (event) => {
  const { request } = event;
  
  // 策略1：缓存优先（Cache First）- 适合静态资源
  event.respondWith(
    caches.match(request).then(response => {
      // 命中缓存则返回，否则请求网络并缓存
      return response || fetch(request).then(fetchResponse => {
        return caches.open(CACHE_NAME).then(cache => {
          cache.put(request, fetchResponse.clone());
          return fetchResponse;
        });
      });
    })
  );
});
```

**常见缓存策略**：

| 策略 | 流程 | 适用场景 |
|------|------|---------|
| **Cache First** | 先查缓存，无则请求网络并缓存 | 静态资源（CSS/JS/图片）|
| **Network First** | 先请求网络，失败则读缓存 | 实时数据（API 请求）|
| **Stale While Revalidate** | 立即返回缓存，同时后台更新 | 新闻、内容页面（兼顾速度和新鲜度）|
| **Network Only** | 只请求网络 | 敏感数据、必须实时的请求 |
| **Cache Only** | 只读缓存 | 离线页面 |

**Stale While Revalidate 实现**：

```javascript
self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.open(CACHE_NAME).then(cache => {
      return cache.match(event.request).then(cachedResponse => {
        // 发起网络请求（无论缓存是否命中）
        const fetchPromise = fetch(event.request).then(networkResponse => {
          // 更新缓存
          cache.put(event.request, networkResponse.clone());
          return networkResponse;
        }).catch(() => cachedResponse); // 网络失败回退缓存
        
        // 立即返回缓存（如果有），否则等待网络
        return cachedResponse || fetchPromise;
      });
    })
  );
});
```

**Workbox（Google 出品，简化 Service Worker）**：

```javascript
// 使用 Workbox 自动生成 Service Worker
import { precacheAndRoute } from 'workbox-precaching';
import { registerRoute } from 'workbox-routing';
import { StaleWhileRevalidate, CacheFirst } from 'workbox-strategies';

// 预缓存构建产物（由构建工具注入清单）
precacheAndRoute(self.__WB_MANIFEST);

// 图片使用 Cache First 策略
registerRoute(
  ({ request }) => request.destination === 'image',
  new CacheFirst({ cacheName: 'images' })
);

// API 使用 Stale While Revalidate
registerRoute(
  ({ url }) => url.pathname.startsWith('/api/'),
  new StaleWhileRevalidate({ cacheName: 'api-cache' })
);
```

**Service Worker 注意事项**：
- **HTTPS 要求**：生产环境必须使用 HTTPS（localhost 开发可 http）
- **作用域限制**：默认只能拦截同目录及子目录下的请求
- **更新机制**：浏览器每 24 小时检查一次 SW 更新，也可手动触发
- **缓存清理**：需要手动管理缓存版本，避免存储膨胀
- **兼容性**：现代浏览器支持良好，IE 不支持

**Cache Storage API（Service Worker 中使用的缓存）**：

```javascript
// 与 HTTP 缓存不同，这是编程式缓存
caches.open('my-cache').then(cache => {
  cache.add('/api/data');        // 请求并缓存
  cache.put('/api/data', response); // 直接存入响应
  cache.match('/api/data');      // 查找缓存
  cache.delete('/api/data');     // 删除缓存
  cache.keys();                  // 列出所有缓存请求
});
```

**调试工具**：
- Chrome DevTools → Application → Service Workers
- 可查看注册状态、跳过等待、模拟离线、查看缓存内容

---

### 11. 垂直居中的方式

```css
/* 1. Flexbox（推荐） */
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* 2. Grid */
.container {
  display: grid;
  place-items: center;
}

/* 3. 绝对定位 + transform */
.center {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

/* 4. 绝对定位 + margin auto */
.center {
  position: absolute;
  top: 0; bottom: 0; left: 0; right: 0;
  margin: auto;
}

/* 5. 表格布局 */
.container {
  display: table-cell;
  text-align: center;
  vertical-align: middle;
}
```

---

### 12. 怎么理解回流和重绘

**回流（Reflow / 重排）**：
- 当元素的大小、位置发生变化，浏览器需要重新计算布局
- 触发条件：元素尺寸变化、内容变化、添加删除元素、窗口大小改变、读取 offset/scroll/client 等属性

**重绘（Repaint）**：
- 当元素的样式发生变化但不影响布局时，浏览器重新绘制元素
- 触发条件：颜色、背景色、可见性（visibility）等变化

**关系**：
- 回流一定会触发重绘，重绘不一定会触发回流
- **优化**：批量修改样式、使用 `className` 代替逐条修改、使用文档碎片（DocumentFragment）、避免频繁读取布局属性

---

### 13. 元素显示和隐藏的方法有哪些

| 方法 | 显示 | 隐藏 | 是否占空间 | 是否触发重排 |
|------|------|------|-----------|------------|
| `display` | `block/inline/flex` | `none` | 不占 | 是 |
| `visibility` | `visible` | `hidden` | 占 | 否（重绘） |
| `opacity` | `1` | `0` | 占 | 否（合成层） |
| `position` | `static` | `absolute; left: -9999px` | 不占 | 是 |
| `clip-path` | `none` | `circle(0)` | 占 | 否 |
| HTML5 `hidden` 属性 | 无 | `hidden` | 不占 | 是 |

---

### 14. 做的比较好的项目，展开讲讲

（建议按**STAR 法则**回答）

**示例框架**：
- **S（Situation）**：项目背景，如"这是一个电商后台管理系统，日活用户 10 万+"
- **T（Task）**：你的职责，如"我负责订单管理和数据可视化模块"
- **A（Action）**：具体行动，如"使用 Vue3 + TypeScript 重构，引入虚拟滚动优化表格性能"
- **R（Result）**：取得成果，如"页面首屏加载时间减少 60%，内存占用降低 40%"

**重点突出**：
- 技术难点及解决方案
- 性能优化手段
- 团队协作经验
- 个人成长收获

---

### 15. 深拷贝和浅拷贝，怎么实现一个深拷贝说思路

**浅拷贝**：只复制一层，深层对象共享引用
- `Object.assign()`、`Array.prototype.slice()`、展开运算符 `{...obj}`

**深拷贝**：完全复制，新旧对象互不影响

**实现思路**：
1. **JSON 方法**（最简单，但有局限）：`JSON.parse(JSON.stringify(obj))`
   - 缺点：丢失函数、undefined、Date、RegExp、循环引用、Symbol

2. **递归实现（详细注释版）**：

```javascript
/**
 * 深拷贝函数 - 递归克隆对象/数组，处理循环引用和特殊类型
 * @param {any} obj - 待克隆的数据
 * @param {WeakMap} hash - 缓存已克隆的对象，解决循环引用问题
 * @returns {any} 克隆后的新数据
 */
function deepClone(obj, hash = new WeakMap()) {
  // 【基准条件 1】处理 null 或原始类型（string/number/boolean/undefined/symbol/bigint）
  // 原始类型直接返回，无需克隆
  if (obj === null || typeof obj !== 'object') {
    return obj;
  }
  
  // 【特殊对象 1】处理 Date 对象
  // Date 存储的是时间戳，创建新实例并传入原时间戳
  if (obj instanceof Date) {
    return new Date(obj);
  }
  
  // 【特殊对象 2】处理 RegExp 正则对象
  // 通过原正则的 source（正则文本）和 flags（标志位）创建新实例
  if (obj instanceof RegExp) {
    return new RegExp(obj.source, obj.flags);
  }
  
  // 【循环引用处理】检查 WeakMap 中是否已克隆过该对象
  // 如果 obj 已经在 hash 中，说明出现了循环引用，直接返回之前创建的克隆对象
  // 这是避免无限递归导致栈溢出的关键
  if (hash.has(obj)) {
    return hash.get(obj);
  }
  
  // 【创建克隆对象】使用原对象的构造函数创建新实例
  // obj.constructor 指向创建 obj 的构造函数（Object/Array/自定义类）
  // 这样可以保留对象的原型链和自定义类类型
  const cloneObj = new obj.constructor();
  
  // 【建立引用映射】在递归前先存入 WeakMap
  // 原因：如果对象内部有循环引用，递归时会先查到这个映射，不会重复克隆
  // WeakMap 的键是对象，值是克隆后的新对象
  hash.set(obj, cloneObj);
  
  // 【递归克隆属性】遍历对象自身的可枚举属性
  for (let key in obj) {
    // hasOwnProperty 过滤掉原型链上的属性，只克隆对象自身的属性
    if (obj.hasOwnProperty(key)) {
      // 递归调用：对每个属性值进行深拷贝
      // 将 hash 传递下去，保证同一个遍历树中共享缓存
      cloneObj[key] = deepClone(obj[key], hash);
    }
  }
  
  // 返回克隆完成的新对象
  return cloneObj;
}
```

**函数执行流程图解**：

```
deepClone(objA)
  ├─ 检查 objA 是否在 hash 中？→ 否
  ├─ 创建 cloneA，hash.set(objA, cloneA)
  ├─ 遍历 objA 的属性
  │   ├─ key1: 原始值 → 直接返回
  │   ├─ key2: 引用 objB
  │   │   └─ deepClone(objB)
  │   │       ├─ 检查 objB 是否在 hash 中？→ 否
  │   │       ├─ 创建 cloneB，hash.set(objB, cloneB)
  │   │       └─ 遍历 objB 的属性...
  │   └─ key3: 引用 objA（循环引用！）
  │       └─ deepClone(objA)
  │           ├─ 检查 objA 是否在 hash 中？→ 是！
  │           └─ 返回 hash.get(objA) → cloneA（避免无限递归）
  └─ 返回 cloneA
```

**关键设计决策解析**：

| 代码设计 | 原因 | 如果不用会怎样 |
|---------|------|--------------|
| `new obj.constructor()` | 保留原型链和自定义类类型 | 数组会被克隆成普通对象，`instanceof Array` 失效 |
| `hash = new WeakMap()` | 缓存已克隆对象，识别循环引用 | 循环引用导致无限递归，栈溢出（Stack Overflow）|
| 先 `hash.set` 再递归 | 对象 A 引用 B，B 又引用 A 时能正确识别 | 先递归再 set 无法处理双向循环引用 |
| `hasOwnProperty` | 不克隆原型链上的方法 | 克隆了不必要的原型方法，污染结果 |
| 单独处理 Date/RegExp | 这类对象不能直接遍历属性克隆 | 丢失时间值或正则标志位 |

**局限性**：
- 不处理函数（函数克隆有争议，通常深拷贝不复制函数）
- 不处理 `Map`、`Set`、`WeakMap`、`WeakSet`
- 不处理 `Symbol` 作为键的属性
- 不处理不可枚举属性
- 生产环境推荐 `lodash.cloneDeep`（处理上述所有边界情况）

---

#### 【补充】WeakMap 详解

**为什么用 WeakMap 而不是 Map 或普通对象？**

**1. WeakMap 的核心特性**

| 特性 | WeakMap | Map | 普通对象 |
|------|---------|-----|---------|
| **键的类型** | 只能是**对象** | 任意类型 | 只能是字符串/Symbol |
| **垃圾回收** | 键是**弱引用**，不影响 GC | 键是强引用，阻止 GC | 强引用 |
| **可遍历** | ❌ 不可遍历（无 size/keys/values） | ✅ 可遍历 | ✅ 可遍历 |
| **性能** | 适合对象→值的映射 | 通用场景 | 简单场景 |

**2. "弱引用"是什么意思？**

弱引用意味着：**如果 WeakMap 的键对象在其他地方不再被引用，垃圾回收器可以直接回收它，不需要先从 WeakMap 中删除**。

```javascript
// 示例：对比 Map 和 WeakMap 的垃圾回收行为
let user = { name: 'Tom' };

// Map：强引用
const map = new Map();
map.set(user, 'user data');
user = null;  // 虽然 user 变量不再引用对象
// 但 Map 仍然强引用该对象 → 对象不会被垃圾回收 → 内存泄漏风险

// WeakMap：弱引用
const weakMap = new WeakMap();
weakMap.set(user, 'user data');
user = null;  // 没有其他强引用 → 垃圾回收器直接回收对象
// WeakMap 中对应的键值对也会自动消失（不可访问）
```

**3. 为什么深拷贝必须用 WeakMap？**

深拷贝过程中，`hash` 的键是**源对象**，值是**克隆后的新对象**。整个深拷贝是一个临时操作，完成后 `hash` 就不再需要了。

如果用 `Map`：
- 深拷贝结束后，`hash` 仍然持有所有源对象的强引用
- 即使外部不再使用源对象，`Map` 中的引用阻止垃圾回收
- 对于大对象，这会造成显著的**内存泄漏**

用 `WeakMap`：
- 深拷贝完成后，函数退出，`hash` 局部变量不再被引用
- WeakMap 中的键如果没有其他强引用，会被垃圾回收
- **不会阻止内存释放，零泄漏风险**

**4. 为什么 WeakMap 不可遍历？**

因为键可能被垃圾回收随时消失，遍历结果是不确定的。

```javascript
const wm = new WeakMap();
let obj = { id: 1 };
wm.set(obj, 'data');

// 以下方法都不存在：
wm.size;        // undefined
wm.keys();      // TypeError
wm.values();    // TypeError
wm.entries();   // TypeError
wm.forEach();   // TypeError

// 唯一可用的方法：
wm.get(obj);    // 'data'
wm.has(obj);    // true
wm.delete(obj); // true
```

**5. WeakMap 的其他应用场景**

**场景 A：为第三方对象添加私有数据（不污染原对象）**

```javascript
const privateData = new WeakMap();

class User {
  constructor(name, password) {
    // 公开属性
    this.name = name;
    // 私有数据存储在 WeakMap 中，外部无法直接访问
    privateData.set(this, { password, loginCount: 0 });
  }
  
  login() {
    const data = privateData.get(this);
    data.loginCount++;
    console.log(`${this.name} 登录了 ${data.loginCount} 次`);
  }
}

const u = new User('Tom', 'secret');
console.log(u.name);        // 'Tom'
console.log(u.password);    // undefined（真正的私有）
u.login();                  // Tom 登录了 1 次
```

**场景 B：DOM 元素关联数据（元素移除后自动释放）**

```javascript
const elementData = new WeakMap();

function attachData(element, data) {
  elementData.set(element, data);
}

const button = document.getElementById('btn');
attachData(button, { clickCount: 0, lastClick: null });

// 当按钮从 DOM 中移除且没有 JS 引用时
// elementData 中的对应条目会被自动垃圾回收，无需手动清理
```

**场景 C：缓存函数计算结果（避免内存无限增长）**

```javascript
const cache = new WeakMap();

function computeExpensive(obj) {
  if (cache.has(obj)) {
    return cache.get(obj);  // 命中缓存
  }
  
  const result = /* 复杂计算 */ obj.data * 2;
  cache.set(obj, result);
  
  return result;
}

// 当 obj 不再被使用时，缓存条目自动释放
// 不会像普通 Map 那样无限累积缓存
```

**6. WeakMap vs WeakSet**

| 特性 | WeakMap | WeakSet |
|------|---------|---------|
| 存储内容 | 键值对（对象→任意值） | 只有键（对象集合） |
| 典型方法 | set/get/has/delete | add/has/delete |
| 用途 | 对象关联数据 | 标记对象是否被处理过 |

```javascript
// WeakSet：标记对象是否已处理
const processed = new WeakSet();

function process(obj) {
  if (processed.has(obj)) {
    console.log('已处理过，跳过');
    return;
  }
  
  // ... 处理逻辑
  processed.add(obj);  // 标记为已处理
}
```

**总结**：WeakMap 是 JavaScript 内存管理的利器，在需要"给对象附加临时数据且不干扰垃圾回收"的场景下（如深拷贝缓存、私有属性、DOM 元数据），它是不可替代的选择。

---

3. **lodash.cloneDeep**（生产环境推荐）

---

### 16. 内存泄露是什么，怎么导致的

**内存泄露**：程序中已分配的内存由于某种原因未释放或无法释放，造成系统内存浪费，可能导致程序变慢或崩溃。

**常见原因**：
1. **意外全局变量**：未声明直接使用变量
2. **闭包**：内部函数持有外部函数变量的引用
3. **定时器未清理**：`setInterval` 未 `clearInterval`
4. **DOM 引用**：移除 DOM 元素但 JS 中仍保留引用
5. **事件监听未移除**：`addEventListener` 未 `removeEventListener`
6. **console.log**：开发环境打印大对象

**排查方法**：
- Chrome DevTools → Performance → Memory → Heap Snapshot
- 对比前后快照，查看 retained size

---

### 17. Promise 介绍

Promise 是异步编程的一种解决方案，用于解决回调地狱问题。

**三种状态**：
- `pending`（进行中）→ `fulfilled`（已成功）/ `rejected`（已失败）
- 状态一旦改变，不可再次改变

**常用方法**：
- `Promise.resolve()` / `Promise.reject()`
- `Promise.all()`：全部成功才成功，一个失败就失败
- `Promise.race()`：返回最快完成的
- `Promise.allSettled()`：等待全部完成，无论成功失败
- `Promise.any()`：任一成功即成功（ES2021）

**注意**：
- `Promise` 是微任务，优先于宏任务执行
- 链式调用中，`then` 返回新的 Promise

---

### 18. Vue 和 React 的区别

| 特性 | Vue | React |
|------|-----|-------|
| 开发团队 | 尤雨溪 / 社区 | Meta（Facebook） |
| 模板语法 | 模板（Template）+ JSX 可选 | JSX |
| 数据绑定 | 双向绑定（v-model） | 单向数据流 |
| 状态管理 | Vuex / Pinia | Redux / MobX / Zustand |
| 学习曲线 | 平缓，文档友好 | 陡峭，生态丰富 |
| 响应式原理 | 依赖收集（Proxy/defineProperty） | 不可变数据 + 手动 setState/useState |
| 组件化 | 单文件组件（SFC） | 函数组件 + Hooks |
| 运行时大小 | 较小 | 较大 |

**共同点**：
- 组件化开发、虚拟 DOM、单向数据流、支持服务端渲染

---

### 19. 数组去重的方法？至少说出 2 种

```javascript
// 1. Set（最简洁）
const unique = [...new Set(arr)];

// 2. filter + indexOf
const unique = arr.filter((item, index) => arr.indexOf(item) === index);

// 3. reduce
const unique = arr.reduce((prev, curr) => 
  prev.includes(curr) ? prev : [...prev, curr], []);

// 4. Map/对象键值对（适合包含对象的情况）
function unique(arr) {
  const map = new Map();
  return arr.filter(item => !map.has(item) && map.set(item, true));
}

// 5. 排序后相邻比较（会改变原数组顺序）
const unique = arr.sort().filter((item, index, self) => 
  index === 0 || item !== self[index - 1]);
```

---

## 二、金山云外包二面

### 1. 深拷贝和浅拷贝，如何实现一个深拷贝

（同上面第15题）

---

### 2. 如何实现一个 new

`new` 运算符做了以下事情：
1. 创建一个空对象
2. 将对象的原型链接到构造函数的 `prototype`
3. 将构造函数中的 `this` 绑定到这个对象
4. 执行构造函数代码
5. 返回这个对象（除非构造函数显式返回对象）

```javascript
function myNew(constructor, ...args) {
  // 1. 创建空对象，原型指向 constructor.prototype
  const obj = Object.create(constructor.prototype);
  // 2. 执行构造函数，绑定 this
  const result = constructor.apply(obj, args);
  // 3. 如果构造函数返回对象则返回该对象，否则返回新对象
  return result instanceof Object ? result : obj;
}

// 测试
function Person(name) {
  this.name = name;
}
const p = myNew(Person, 'Tom');
console.log(p.name); // Tom
console.log(p instanceof Person); // true
```

---

### 3. 怎么理解回流和重绘

（同上面第12题）

---

### 4. 前端如何做性能优化

（同上面第10题）

---

### 5. 平常是如何学习前端的？

**建议回答框架**：
- **官方文档**：MDN、Vue/React 官方文档优先
- **技术博客**：掘金、知乎、Dev.to、CSS-Tricks
- **GitHub**：阅读源码、参与开源、关注 trends
- **实践项目**：边学边做，用新技术重构旧项目
- **技术社区**：参加技术分享、前端大会、加入技术社群
- **笔记总结**：写博客、做思维导图、整理面试题
- **英文能力**：阅读英文文档，关注 TC39、W3C 规范

---

### 6. 微前端了解吗？

**微前端**：将前端应用拆分成多个独立的子应用，每个子应用可以独立开发、独立部署。

**核心思想**：借鉴微服务架构，解决巨石应用（Monolith）的维护难题。

**常见方案**：
- **qiankun**（阿里）：基于 single-spa，使用简单，生态成熟
- **single-spa**：最早期的微前端框架
- **Module Federation**（Webpack 5）：模块联邦，运行时共享依赖
- **iframe**：简单隔离，但体验差
- **Web Components**：原生组件化方案

**解决的问题**：
- 技术栈无关（Vue/React/Angular 混用）
- 独立开发部署
- 代码隔离（JS 沙箱、样式隔离）

---

#### 【补充】微前端开源项目推荐与实战指南

**1. 主流开源方案对比与推荐**

| 方案 | 维护方 | 特点 | 适用场景 | GitHub Stars |
|------|--------|------|---------|-------------|
| **qiankun** | 阿里蚂蚁 | 基于 single-spa，JS 沙箱完善，中文文档友好 | **国内首选**，中大型项目，快速上手 | 15k+ |
| **Micro App** | 京东 | 类 WebComponent 方案，零依赖，侵入性低 | 需要轻量方案，不想改造子应用 | 5k+ |
| **Module Federation** | Webpack 官方 | 运行时模块共享，无中心容器概念 | Webpack 生态，需要共享组件/库 | 内置 Webpack5 |
| **single-spa** | 社区 | 最早期框架，生态丰富但配置复杂 | 需要高度定制，有专研团队 | 12k+ |
| **wujie** | 腾讯 | 基于 WebComponent + iframe，隔离彻底 | 需要极致隔离，复杂子应用 | 4k+ |
| **Garfish** | 字节跳动 | 类似 qiankun，性能优化较好 | 字节系技术栈，需要高性能 | 3k+ |

**推荐选择**：
- **新手/快速接入**：选 **qiankun**（生态最成熟，问题最少）
- **极致隔离需求**：选 **wujie**（iframe 隔离 + WebComponent 通信）
- **Webpack 统一技术栈**：选 **Module Federation**（无中心容器，共享依赖最优雅）
- **不想侵入子应用**：选 **Micro App**（类似 iframe 用法，通过自定义元素加载）

---

**2. 微前端项目目录结构（以 qiankun 为例）**

**单体仓库（Monorepo）推荐结构**：

```
micro-frontend-project/          # 项目根目录
├── package.json                 # 根 package.json，管理公共依赖和 scripts
├── pnpm-workspace.yaml          # pnpm 工作区配置（Monorepo 推荐）
├── README.md                    # 项目说明
├── .gitignore
│
├── apps/                        # 所有应用目录
│   ├── main/                    # 【基座/主应用】唯一入口
│   │   ├── package.json
│   │   ├── vite.config.ts       # 或 vue.config.js / webpack.config.js
│   │   ├── public/
│   │   ├── src/
│   │   │   ├── main.ts          # 入口文件，注册微应用
│   │   │   ├── App.vue          # 主应用布局（含导航 + 微应用挂载点）
│   │   │   ├── micro/           # 微前端相关配置
│   │   │   │   ├── apps.ts      # 微应用注册表
│   │   │   │   ├── lifecycle.ts # 全局生命周期钩子
│   │   │   │   └── utils.ts     # 微前端工具函数
│   │   │   ├── router/          # 主应用路由（处理子应用路由前缀）
│   │   │   ├── store/           # 主应用状态（如全局用户态）
│   │   │   ├── components/      # 公共组件（Layout、Header、Sidebar）
│   │   │   └── shared/          # 共享模块（定义子应用通信接口）
│   │   └── index.html
│   │
│   ├── sub-vue/                 # 【子应用 1】Vue 技术栈
│   │   ├── package.json
│   │   ├── vite.config.ts
│   │   ├── src/
│   │   │   ├── main.ts          # 导出 qiankun 生命周期钩子
│   │   │   ├── App.vue
│   │   │   ├── router/          # 子应用独立路由（history/base 配置）
│   │   │   ├── store/           # 子应用独立状态管理
│   │   │   ├── views/           # 页面
│   │   │   └── public-path.ts   # 动态设置 __webpack_public_path__
│   │   └── index.html
│   │
│   ├── sub-react/               # 【子应用 2】React 技术栈
│   │   ├── package.json
│   │   ├── craco.config.js      # 或 vite.config.ts
│   │   ├── src/
│   │   │   ├── index.tsx        # 导出 qiankun 生命周期
│   │   │   ├── App.tsx
│   │   │   ├── router/          # React Router 配置
│   │   │   ├── pages/           # 页面
│   │   │   └── public-path.js
│   │   └── index.html
│   │
│   └── sub-legacy/              # 【子应用 3】遗留系统（jQuery/纯 HTML）
│       ├── package.json
│       └── src/
│           └── index.html       # 通过 qiankun 加载 HTML 入口
│
├── packages/                    # 【共享包】Monorepo 内部共享
│   ├── shared-utils/            # 工具函数库
│   │   ├── package.json
│   │   └── src/
│   │       ├── index.ts
│   │       ├── request.ts       # 统一请求封装
│   │       └── storage.ts       # 统一存储封装
│   │
│   ├── shared-ui/               # 公共 UI 组件库
│   │   ├── package.json
│   │   └── src/
│   │       ├── components/      # Button、Modal、Table 等
│   │       └── index.ts
│   │
│   └── shared-types/            # 公共类型定义
│       ├── package.json
│       └── src/
│           ├── user.ts          # 用户相关类型
│           └── api.ts           # API 接口类型
│
├── scripts/                     # 构建/部署脚本
│   ├── build-all.sh             # 批量构建所有应用
│   ├── deploy.sh                # 部署脚本
│   └── verify-subapps.js        # 子应用健康检查
│
└── docker/                      # Docker 配置（可选）
    ├── nginx.conf               # Nginx 配置（路由分发）
    └── Dockerfile
```

**多仓库（Multirepo）结构**：

```
# 独立仓库，各自管理
main-app/                        # 主应用仓库
  ├── src/
  │   └── micro-apps.ts          # 配置子应用的入口 URL（非本地路径）
  └── package.json

sub-vue/                         # Vue 子应用仓库（独立部署）
  ├── src/
  │   └── main.ts                # 导出生命周期
  └── package.json

sub-react/                       # React 子应用仓库（独立部署）
  ├── src/
  │   └── index.tsx
  └── package.json
```

**Monorepo vs Multirepo 选择**：

| 维度 | Monorepo（单体仓库） | Multirepo（多仓库） |
|------|---------------------|---------------------|
| **代码共享** | 方便，直接本地引用 | 需发布 npm 包或 git submodule |
| **版本管理** | 统一版本，一键发布 | 各应用独立版本 |
| **CI/CD** | 复杂，需精确判断变更范围 | 简单，各仓库独立流水线 |
| **团队协作** | 权限管理复杂（Code Owner） | 天然隔离，权限清晰 |
| **适用规模** | 中小型团队（< 50人） | 大型团队，各组独立 |

**推荐**：中小型团队用 **Monorepo + pnpm workspace**，大型团队用 **Multirepo + npm 私有仓库**。

---

**3. 微前端如何管理（工程化实践）**

**（1）应用注册与路由管理**

```typescript
// apps/main/src/micro/apps.ts
import { registerMicroApps, start } from 'qiankun';

// 子应用注册表：开发环境/测试环境/生产环境配置不同入口
const apps = [
  {
    name: 'sub-vue',              // 唯一标识
    entry: '//localhost:8081',    // 开发环境入口
    // entry: '/sub-apps/vue/',   // 生产环境入口（Nginx 代理路径）
    container: '#subapp-container', // DOM 挂载点
    activeRule: '/vue',           // 激活路由规则
    props: {                      // 向子应用传递数据
      baseApi: '/api/vue',
      sharedStore: mainStore,     // 传递主应用状态（谨慎使用）
    }
  },
  {
    name: 'sub-react',
    entry: '//localhost:8082',
    container: '#subapp-container',
    activeRule: '/react',
    props: { baseApi: '/api/react' }
  },
  {
    name: 'sub-legacy',
    entry: '//localhost:8083',
    container: '#subapp-container',
    activeRule: '/legacy',
  }
];

registerMicroApps(apps, {
  // 全局生命周期
  beforeLoad: (app) => console.log('加载前', app.name),
  afterMount: (app) => console.log('挂载后', app.name),
});

start({
  sandbox: {
    strictStyleIsolation: true,   // Shadow DOM 样式隔离（实验性）
    experimentalStyleIsolation: true, // 样式作用域转换
  }
});
```

**（2）子应用改造（以 Vue3 为例）**

```typescript
// apps/sub-vue/src/main.ts
import { createApp } from 'vue';
import App from './App.vue';
import router from './router';
import { renderWithQiankun, qiankunWindow } from 'vite-plugin-qiankun/dist/helper';

let app: ReturnType<typeof createApp> | null = null;

// 判断是否在 qiankun 环境中运行
const isQiankun = qiankunWindow.__POWERED_BY_QIANKUN__;

function render(props: any = {}) {
  const { container } = props;
  
  app = createApp(App);
  app.use(router);
  
  // 挂载到主应用提供的容器，或独立运行时挂载到 document
  app.mount(container ? container.querySelector('#app') : '#app');
}

// 独立运行时直接渲染
if (!isQiankun) {
  render();
}

// 导出 qiankun 生命周期（必须）
renderWithQiankun({
  // 挂载
  mount(props) {
    console.log('子应用挂载', props);
    render(props);
  },
  // 卸载
  unmount() {
    app?.unmount();
    app = null;
  },
  // 更新（手动触发时）
  update(props) {
    console.log('子应用更新', props);
  },
  // bootstrap 只会在微应用初始化的时候调用一次
  bootstrap() {
    console.log('子应用初始化');
  }
});
```

**关键改造点**：
1. 路由 base 配置：`history: createWebHistory(isQiankun ? '/vue' : '/')`
2. 打包配置：`base: '/sub-apps/vue/'`（生产环境子应用部署路径）
3. `__webpack_public_path__` 或 `vite` 的 `base` 配置（确保动态加载资源路径正确）

**（3）子应用间通信管理**

```typescript
// 方案 1：基于 props 的父子通信（推荐，最简单）
// 主应用传递 actions 给子应用
const actions = {
  onGlobalStateChange: (callback: Function) => { /* 订阅全局状态 */ },
  setGlobalState: (state: object) => { /* 修改全局状态 */ }
};

// 子应用接收
mount(props) {
  props.onGlobalStateChange((state, prev) => {
    console.log('主应用状态变化', state);
  });
  props.setGlobalState({ user: { name: 'Tom' } });
}

// 方案 2：Event Bus（简单场景）
// shared-utils/src/eventBus.ts
class EventBus {
  private events: Map<string, Function[]> = new Map();
  
  on(event: string, callback: Function) {
    if (!this.events.has(event)) this.events.set(event, []);
    this.events.get(event)!.push(callback);
  }
  
  emit(event: string, data?: any) {
    this.events.get(event)?.forEach(cb => cb(data));
  }
}

export const eventBus = new EventBus();

// 方案 3：共享状态库（复杂场景）
// 使用 RxJS 或 Zustand 创建跨应用状态
import { create } from 'zustand';
import { subscribeWithSelector } from 'zustand/middleware';

export const useSharedStore = create(subscribeWithSelector(() => ({
  user: null,
  permissions: [],
  theme: 'light',
})));

// 主应用初始化
useSharedStore.setState({ user: currentUser });

// 子应用订阅（跨技术栈可用，Zustand 是 vanilla JS）
useSharedStore.subscribe(
  state => state.user,
  user => console.log('用户变化', user)
);
```

**（4）样式隔离方案**

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **CSS Modules** | 编译时添加哈希类名 | 无运行时开销 | 需要改造现有代码 |
| **BEM 命名规范** | 约定式命名（block-element-modifier） | 简单，无需工具 | 靠人为遵守 |
| **PostCSS Prefix** | 构建时自动添加前缀 `.sub-vue-app .btn {}` | 自动处理 | 增加选择器长度 |
| **Shadow DOM** | 浏览器原生隔离 | 隔离彻底 | qiankun 中性能较差，事件穿透问题 |
| **qiankun 沙箱** | 运行时改写选择器 | 自动，无需改造 | 动态插入样式可能有延迟 |

**推荐实践**：
- 子应用统一使用 **CSS Modules** 或 **BEM**
- 主应用使用 **PostCSS Prefix** 做兜底
- 第三方 UI 库（Element/Ant Design）通过构建工具修改前缀：`prefixCls: 'sub1-ant'`

**（5）公共依赖共享**

```javascript
// qiankun 配置共享依赖（通过 externals + CDN 或 Module Federation）
// 方案：子应用打包时排除公共库，由主应用加载

// apps/sub-vue/vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      external: ['vue', 'vue-router', 'axios'], // 不打入包内
      output: {
        paths: {
          vue: 'https://cdn.jsdelivr.net/npm/vue@3.2.0/dist/vue.esm-browser.js'
        }
      }
    }
  }
});

// 更优雅的方案：Module Federation
// 主应用暴露共享模块
const ModuleFederationPlugin = require('webpack').container.ModuleFederationPlugin;

new ModuleFederationPlugin({
  name: 'main_app',
  shared: {
    vue: { singleton: true, eager: true },
    'vue-router': { singleton: true },
    axios: { singleton: true }
  }
});

// 子应用引用共享模块
new ModuleFederationPlugin({
  name: 'sub_vue',
  shared: {
    vue: { singleton: true },
    'vue-router': { singleton: true }
  }
});
```

**（6）部署与 Nginx 配置**

```nginx
# nginx.conf - 微前端部署核心配置
server {
    listen 80;
    server_name my-app.com;
    
    # 主应用
    location / {
        root /var/www/main-app/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # Vue 子应用
    location /sub-apps/vue {
        alias /var/www/sub-vue/dist;
        try_files $uri $uri/ /sub-apps/vue/index.html;
        
        # 解决跨域（子应用资源加载）
        add_header Access-Control-Allow-Origin *;
    }
    
    # React 子应用
    location /sub-apps/react {
        alias /var/www/sub-react/dist;
        try_files $uri $uri/ /sub-apps/react/index.html;
        add_header Access-Control-Allow-Origin *;
    }
    
    # API 代理
    location /api {
        proxy_pass http://backend-server:8080;
        proxy_set_header Host $host;
    }
}
```

**（7）开发环境配置**

```json
// 根 package.json - 一键启动所有应用
{
  "scripts": {
    "install:all": "pnpm install",
    "dev": "pnpm run --parallel dev",        // 并行启动所有应用的 dev
    "dev:main": "pnpm --filter main dev",
    "dev:vue": "pnpm --filter sub-vue dev",
    "dev:react": "pnpm --filter sub-react dev",
    "build": "pnpm run -r build",            // 递归构建所有应用
    "build:main": "pnpm --filter main build",
    "lint": "pnpm run -r lint",
    "test": "pnpm run -r test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}

// pnpm-workspace.yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

---

**4. 微前端落地注意事项**

| 问题 | 解决方案 |
|------|---------|
| **子应用 CSS 污染全局** | CSS Modules + BEM + 构建时自动加前缀 |
| **JS 全局变量冲突** | qiankun 沙箱隔离 + 避免在 window 上挂载变量 |
| **路由冲突** | 主应用路由管理基座，子应用路由 base 加前缀 |
| **公共依赖重复加载** | Module Federation 或 externals + CDN |
| **登录态共享** | Cookie（Domain 设置）或主应用传递 Token |
| **子应用独立部署后主应用报错** | 子应用健康检查 + 灰度发布 + 快速回滚 |
| **调试困难** | Chrome DevTools → Application → Frames 查看各子应用 |
| **性能问题** | 子应用懒加载、路由级别的动态加载、公共依赖共享 |

**不推荐微前端的场景**：
- 团队规模小（< 20人），一个应用能搞定
- 技术栈完全统一，无历史包袱
- 对首屏性能要求极高（微前端有额外运行时开销）
- 子应用间需要频繁、复杂的实时数据同步

---

### 7. nginx 有了解吗，如何用 nginx 做重定向

Nginx 是高性能的 HTTP 和反向代理服务器。

**重定向配置**：

```nginx
# 1. 永久重定向（301）
server {
    listen 80;
    server_name old.com;
    return 301 $scheme://new.com$request_uri;
}

# 2. rewrite 重定向
server {
    listen 80;
    location /old-path {
        rewrite ^/old-path(.*)$ /new-path$1 permanent;
    }
}

# 3. 反向代理
location /api {
    proxy_pass http://localhost:3000;
    proxy_set_header Host $host;
}
```

**Nginx 常用功能**：
- 反向代理、负载均衡
- 静态资源服务
- HTTPS 配置、Gzip 压缩
- 限流、防盗链

---

### 8. 跨域是什么？怎么解决？jsonp 方式是如何做的

**跨域（CORS）**：浏览器的同源策略限制，协议、域名、端口任一不同即为跨域。

**解决方案**：
1. **CORS**（跨域资源共享）：服务端设置响应头
   ```
   Access-Control-Allow-Origin: *
   Access-Control-Allow-Methods: GET, POST
   ```
2. **JSONP**：利用 `<script>` 标签不受同源策略限制
3. **代理服务器**：开发环境用 webpack devServer proxy，生产环境用 Nginx
4. **postMessage**：窗口间通信
5. **WebSocket**：不受同源策略限制

**JSONP 原理**：
1. 前端定义回调函数 `handleData(data)`
2. 创建 `<script>` 标签，src 带上回调函数名 `?callback=handleData`
3. 服务端返回 `handleData({"name": "Tom"})`
4. 脚本加载后执行回调函数

```javascript
function jsonp(url, callbackName) {
  return new Promise((resolve) => {
    const script = document.createElement('script');
    window[callbackName] = (data) => {
      resolve(data);
      document.body.removeChild(script);
    };
    script.src = `${url}?callback=${callbackName}`;
    document.body.appendChild(script);
  });
}
```

**JSONP 缺点**：只支持 GET 请求，存在 XSS 安全隐患

---

### 9. 事件循环说说，宏任务和微任务有哪些

（同上面第9题）

**补充**：

```javascript
console.log('1');
setTimeout(() => console.log('2'), 0);
Promise.resolve().then(() => console.log('3'));
console.log('4');
// 输出：1 4 3 2
```

执行顺序：
1. 同步代码：`1`、`4`
2. 微任务队列：`3`
3. 宏任务队列：`2`

---

### 10. this 指向问题

`this` 的指向在函数执行时确定：

1. **默认绑定**：普通函数调用，`this` 指向全局对象（严格模式为 `undefined`）
2. **隐式绑定**：对象方法调用，`this` 指向调用对象
3. **显式绑定**：`call`、`apply`、`bind`，`this` 指向指定对象
4. **new 绑定**：构造函数调用，`this` 指向新创建的实例
5. **箭头函数**：没有自己的 `this`，继承外层作用域

```javascript
const obj = {
  name: 'Tom',
  say() { console.log(this.name); },
  sayArrow: () => { console.log(this.name); }
};
obj.say();      // Tom（隐式绑定）
obj.sayArrow(); // undefined（继承全局，或严格模式下报错）
```

---

### 11. 了解 axios 的原理吗？怎么实现的

Axios 是基于 Promise 的 HTTP 客户端，同时支持浏览器和 Node.js。

**核心原理**：
1. **浏览器端**：基于 XMLHttpRequest
2. **Node.js 端**：基于 http 模块

**核心功能实现**：
- **拦截器**：使用数组存储请求/响应拦截器，promise 链式调用
- **取消请求**：基于 XMLHttpRequest 的 abort() 或 CancelToken
- **并发请求**：`axios.all()` 封装 `Promise.all()`
- **自动转换 JSON**：默认 `transformRequest` / `transformResponse`

**简单实现**：

```javascript
class Axios {
  request(config) {
    return new Promise((resolve, reject) => {
      const xhr = new XMLHttpRequest();
      xhr.open(config.method || 'get', config.url);
      xhr.onreadystatechange = () => {
        if (xhr.readyState === 4) {
          if (xhr.status >= 200 && xhr.status < 300) {
            resolve(xhr.response);
          } else {
            reject(xhr.statusText);
          }
        }
      };
      xhr.send(config.data);
    });
  }
}
```

---

### 12. JS 有哪些数据类型

**原始类型（Primitive）**：
- `string`、`number`、`boolean`、`null`、`undefined`
- `symbol`（ES6）、`bigint`（ES2020）

**引用类型（Reference）**：
- `object`（包括普通对象、数组、函数、日期、正则等）

**判断方法**：
- `typeof`：适合判断原始类型，但 `typeof null === 'object'`（历史 bug）
- `instanceof`：判断原型链，适合判断引用类型
- `Object.prototype.toString.call()`：最准确，返回 `[object Type]`
- `Array.isArray()`：判断数组

---

## 三、盛天网络和东高科技

### 1. 原型和原型链

（同上面第8题）

---

### 2. webpack 的构建流程

Webpack 构建流程分为以下阶段：

1. **初始化参数**：合并配置文件和命令行参数
2. **开始编译**：创建 Compiler 对象，加载插件
3. **确定入口**：根据 entry 找到入口文件
4. **编译模块**：
   - 调用 loader 将模块转为标准 JS
   - 使用 acorn 解析为 AST
   - 分析依赖关系，递归编译依赖模块
5. **完成编译**：得到模块的依赖关系图（Dependency Graph）
6. **输出资源**：
   - 根据 entry 和 module 生成 Chunk
   - 使用 template 生成最终代码
7. **输出完成**：将文件写入 output 指定的目录

**关键概念**：
- Entry、Output、Loader、Plugin、Mode
- Chunk：代码块，根据 entry 或代码分割生成
- Bundle：最终输出的文件

---

### 3. 说说轮播图的思路

**基础实现**：
1. 容器设置 `overflow: hidden`，内部用 flex 横向排列
2. 通过 `transform: translateX()` 或改变 `left` 值切换
3. 使用 `setInterval` 或 `requestAnimationFrame` 自动轮播
4. 监听过渡结束事件（transitionend）

**关键细节**：
- **无限循环**：首尾各克隆一张，滑动到克隆位时瞬间跳回真实位
- **手势支持**：监听 touchstart / touchmove / touchend，计算滑动距离
- **性能优化**：使用 `transform` 开启 GPU 加速，避免重排
- **懒加载**：只加载当前及前后图片

**进阶方案**：
- 使用 Swiper.js（成熟的开源库）
- 虚拟列表思想（大量图片时）

---

### 4. redux 的工作流程

Redux 是 JavaScript 状态容器，遵循三大原则：
- 单一数据源（Single source of truth）
- State 只读（State is read-only）
- 使用纯函数修改（Changes are made with pure functions）

**工作流程**：

```
Action → Dispatch → Reducer → Store → View
   ↑_____________________________________|
```

1. **Action**：描述发生了什么的普通对象 `{ type: 'ADD', payload: 1 }`
2. **Reducer**：纯函数，接收旧 state 和 action，返回新 state
3. **Store**：整个应用的 state 管理中心
   - `store.getState()` 获取状态
   - `store.dispatch(action)` 触发更新
   - `store.subscribe(listener)` 订阅变化

**中间件**：
- Redux-Thunk：处理异步 action
- Redux-Saga：使用 Generator 处理复杂异步流程

---

### 5. http 缓存有哪些

（同上面第5题）

---

### 6. websocket 的底层协议

WebSocket 是基于 TCP 的**全双工通信协议**，在 HTTP 协议上升级而来。

**连接过程**：
1. 客户端发送 HTTP 请求，带上升级头：
   ```
   Connection: Upgrade
   Upgrade: websocket
   Sec-WebSocket-Key: xxx（Base64 编码的随机值）
   ```
2. 服务端返回 101 Switching Protocols，同意升级
3. 连接建立，之后使用 ws:// 或 wss:// 通信

**特点**：
- 全双工：客户端和服务端可同时发送消息
- 低延迟：建立连接后无需重复握手
- 轻量头部：数据帧头部仅 2-14 字节
- 支持二进制：可传输文本和二进制数据

**心跳机制**：防止长时间无通信被中间代理断开
- 客户端定时发送 Ping 帧，服务端回复 Pong 帧

---

### 7. Vue2 和 Vue3 的 tree shaking 的区别

**Tree Shaking**：消除未使用代码的优化技术，依赖 ES Module 的静态结构。

**Vue 2**：
- 使用 UMD 和 IIFE 构建，难以 tree shaking
- 全局 API 挂载在 Vue 构造函数上（`Vue.set`、`Vue.nextTick`）
- 即使未使用也会被打包

**Vue 3**：
- 全面采用 ES Module 构建
- API 改为具名导出（`import { nextTick } from 'vue'`）
- 未使用的 API 不会被打包
- 按需引入，包体积显著减小（运行时约 10KB gzipped）

**Vue 3 的优化**：
- 编译时优化：静态提升、PatchFlag 标记动态节点
- 源码采用 Monorepo，各模块独立发布

---

### 8. 为什么要减少 http 的请求

**HTTP 请求的开销**：
1. **建立连接**：TCP 三次握手（HTTPS 还需 TLS 握手）
2. **队头阻塞**：HTTP/1.1 同一域名并行请求数受限（6-8 个）
3. **请求头开销**：每次请求都携带 Cookie、User-Agent 等头部
4. **服务器压力**：每个请求都消耗服务器资源

**优化手段**：
- 文件合并（CSS/JS 合并）
- CSS Sprites（雪碧图）
- 内联小资源（Base64）
- 使用 HTTP/2（多路复用解决部分问题）
- 域名分片（HTTP/1.1 时代）

---

### 9. vue 的双向数据绑定的原理

**Vue 2**：
- 使用 `Object.defineProperty()` 劫持对象属性的 getter/setter
- 数据变化时通知依赖（Dep），触发 Watcher 更新视图
- 数组通过重写 `push`、`pop`、`splice` 等方法实现响应式
- **缺点**：无法检测对象新增/删除属性，需使用 `Vue.set` / `Vue.delete`

**Vue 3**：
- 使用 `Proxy` 代理整个对象
- 可监听对象新增/删除属性、数组索引变化
- 性能更好，初始化时无需递归遍历所有属性

**v-model 本质**：
- 语法糖，相当于 `:value` + `@input`（Vue 2）或 `:modelValue` + `@update:modelValue`（Vue 3）

---

### 10. 前端性能优化

（同上面第10题）

---

### 11. 事件循环

（同上面第9题）

---

### 12. 箭头函数

（同上面第3题）

---

### 13. react 中组件传值

**父子组件通信**：
- Props 向下传递
- Callback 向上传递（父传子函数，子调用传值）

**跨级组件通信**：
- Context API：`React.createContext` + `Provider` / `Consumer`
- 状态管理库（Redux、Zustand）

**兄弟组件通信**：
- 状态提升到共同父组件
- 事件总线（Event Bus）
- 状态管理库

**其他方式**：
- Ref：`useImperativeHandle` + `forwardRef`（暴露子组件方法）
- URL 参数、LocalStorage（全局共享）

---

### 14. 虚拟 DOM 的理解

**虚拟 DOM（Virtual DOM）**：
- 用 JavaScript 对象描述真实 DOM 结构的轻量级表示
- 是真实 DOM 的抽象，与平台无关（可用于浏览器、Native、小程序等）

**工作流程**：
1. 用 JS 对象构建虚拟 DOM 树
2. 状态变化时生成新的虚拟 DOM
3. 通过 Diff 算法对比新旧虚拟 DOM
4. 找出最小变更集（Patch）
5. 批量更新真实 DOM

**优点**：
- 减少直接操作真实 DOM 的次数（操作 DOM 是昂贵的）
- 跨平台（React Native、Weex）
- 批量更新，减少重排重绘

**缺点**：
- 首次渲染不如直接操作 DOM 快
- 需要额外的内存存储虚拟 DOM
- Diff 算法也有计算开销

**Vue 的优化**：
- 编译时优化，标记动态节点（PatchFlag），减少比较范围
- 静态提升：静态节点只创建一次

---

#### 【补充】Diff 算法原理详解

**1. 为什么需要 Diff 算法**

状态变化后生成新的虚拟 DOM 树，如果直接遍历两棵树逐个节点对比，时间复杂度是 **O(n³)**（树形结构对比的经典算法复杂度），这对于前端渲染来说是不可接受的。

Diff 算法通过**三个核心假设**将复杂度优化到 **O(n)**：
1. **同层比较**：只对比同一层的节点，不跨层级比较
2. **类型不同则直接替换**：如果节点类型不同（如 `div` 变成 `span`），直接销毁旧树、创建新树
3. **通过 key 标识同一组子节点**：列表渲染时，用 `key` 判断节点是否相同，实现复用

---

**2. Diff 算法执行流程（以 Vue 2 的双端 Diff 为例）**

**对比两个虚拟 DOM 树的流程**：

```
新 VNode 树                     旧 VNode 树
    div                            div
   /   \                          /   \
  p     span        Diff         p     div   ← 类型不同，直接替换
  |      |          ===>         |      |
 text   text                    text   text
```

**同层级节点的四种对比情况**：

| 场景 | 处理方式 | 示例 |
|------|---------|------|
| **新旧都是文本节点** | 直接替换文本内容 | `<div>旧</div>` → `<div>新</div>` |
| **新旧节点类型不同** | 销毁旧节点，创建并挂载新节点 | `<div>` → `<span>` |
| **新旧节点类型相同** | 对比属性差异，复用 DOM 节点，递归对比子节点 | `class="a"` → `class="b"` |
| **新旧都是数组（子节点列表）** | 进入**子节点 Diff**（最复杂） | 列表增删改顺序 |

---

**3. 子节点 Diff 的核心：双端比较算法**

当新旧子节点都是数组时（如 `v-for` 渲染的列表），Diff 算法采用**双端比较**策略：

依次采用四种策略：

	- 旧头 - 新头，复用节点，双指针++
	- 旧尾-新尾，复用节点，双指针--
	- 旧头-新尾，复用节点，oldStart元素移动到oldEnd后，oldStart位置设置为undefinded，双指针内收
	- 旧尾-新头，复用节点，，oldEnd元素移动到oldStart后，oldEnd位置设置为undefinded，双指针内收
	- 在就数组遍历寻找该节点，找到则移动该元素到就旧数组头部（找不到在旧数组尾部创建）
 - 循环结束
   	- 新数组有剩余，批量创建
   	- 旧数组有剩余，批量删除

```javascript
// 伪代码：双端 Diff 核心逻辑
function updateChildren(parentElm, oldCh, newCh) {
  let oldStartIdx = 0;            // 旧数组头指针
  let oldEndIdx = oldCh.length - 1; // 旧数组尾指针
  let newStartIdx = 0;            // 新数组头指针
  let newEndIdx = newCh.length - 1; // 新数组尾指针
  
  let oldStartVnode = oldCh[0];
  let oldEndVnode = oldCh[oldEndIdx];
  let newStartVnode = newCh[0];
  let newEndVnode = newCh[newEndIdx];
  
  while (oldStartIdx <= oldEndIdx && newStartIdx <= newEndIdx) {
    // 四种命中策略（按优先级排序）
    
    // 策略 1：旧头 === 新头（最常用，如尾部追加）
    if (sameVnode(oldStartVnode, newStartVnode)) {
      patchVnode(oldStartVnode, newStartVnode); // 递归对比属性和子节点
      oldStartVnode = oldCh[++oldStartIdx];
      newStartVnode = newCh[++newStartIdx];
    }
    // 策略 2：旧尾 === 新尾（头部插入场景）
    else if (sameVnode(oldEndVnode, newEndVnode)) {
      patchVnode(oldEndVnode, newEndVnode);
      oldEndVnode = oldCh[--oldEndIdx];
      newEndVnode = newCh[--newEndIdx];
    }
    // 策略 3：旧头 === 新尾（移动旧头到尾部）
    else if (sameVnode(oldStartVnode, newEndVnode)) {
      patchVnode(oldStartVnode, newEndVnode);
      parentElm.insertBefore(oldStartVnode.elm, oldEndVnode.elm.nextSibling);
      oldStartVnode = oldCh[++oldStartIdx];
      newEndVnode = newCh[--newEndIdx];
    }
    // 策略 4：旧尾 === 新头（移动旧尾到头部）
    else if (sameVnode(oldEndVnode, newStartVnode)) {
      patchVnode(oldEndVnode, newStartVnode);
      parentElm.insertBefore(oldEndVnode.elm, oldStartVnode.elm);
      oldEndVnode = oldCh[--oldEndIdx];
      newStartVnode = newCh[++newStartIdx];
    }
    // 策略 5：以上都不命中，遍历旧数组查找可复用节点
    else {
      const idxInOld = findIdxInOld(newStartVnode, oldCh, oldStartIdx, oldEndIdx);
      if (idxInOld === -1) {
        // 新节点在旧数组中不存在，创建新 DOM
        createElm(newStartVnode, parentElm, oldStartVnode.elm);
      } else {
        // 找到可复用节点，移动到头部
        const vnodeToMove = oldCh[idxInOld];
        patchVnode(vnodeToMove, newStartVnode);
        parentElm.insertBefore(vnodeToMove.elm, oldStartVnode.elm);
        oldCh[idxInOld] = undefined; // 标记为已处理
      }
      newStartVnode = newCh[++newStartIdx];
    }
  }
  
  // 处理剩余节点
  if (oldStartIdx > oldEndIdx) {
    // 新数组有剩余，批量创建
    addVnodes(parentElm, newCh, newStartIdx, newEndIdx);
  } else if (newStartIdx > newEndIdx) {
    // 旧数组有剩余，批量删除
    removeVnodes(parentElm, oldCh, oldStartIdx, oldEndIdx);
  }
}

// 判断两个节点是否相同（复用 DOM 的前提）
function sameVnode(a, b) {
  return (
    a.key === b.key &&           // key 必须相同
    a.tag === b.tag &&           // 标签类型相同（div/span/p）
    a.isComment === b.isComment && // 同为注释节点
    isDef(a.data) === isDef(b.data) // 都有/都没有属性数据
  );
}
```

**四种命中策略图解**：

```
旧子节点: [A, B, C, D]      新子节点: [B, A, C, E]
            ↑  ↑                      ↑  ↑
         头指针 尾指针               头指针 尾指针

第 1 轮:
  策略 1: oldStart(A) vs newStart(B) → key 不同，不命中
  策略 2: oldEnd(D) vs newEnd(E) → key 不同，不命中
  策略 3: oldStart(A) vs newEnd(E) → key 不同，不命中
  策略 4: oldEnd(D) vs newStart(B) → key 不同，不命中
  策略 5: 在旧数组中查找 newStart(B)，找到索引 1
         → 移动旧 B 到头部，旧数组标记为 [A, undefined, C, D]
         新头指针右移

旧子节点: [A, undefined, C, D]  新子节点: [B, A, C, E]
                ↑    ↑                   ↑     ↑
             头指针  尾指针            头指针   尾指针

第 2 轮:
  策略 1: oldStart(A) vs newStart(A) → key 相同，命中！
         → 复用 A 的 DOM，递归 patch，双头指针右移
         ...（继续循环直到结束）
```





**为什么优先用双端比较而不是简单遍历？**

- 实际业务中，列表操作多为**尾部追加**、**头部插入**、**逆序**
- 双端比较能在 **O(n)** 内处理这些场景
- 简单遍历 + 查找需要 **O(n²)** 复杂度

---

**4. Key 的作用与重要性**

**key 是 Diff 算法的"身份证"**：

```html
<!-- 没有 key：Vue 只能按位置对比 -->
<div v-for="item in list">
  {{ item.name }}
</div>

<!-- 有 key：Vue 按 key 查找复用节点 -->
<div v-for="item in list" :key="item.id">
  {{ item.name }}
</div>
```

**用 index 作为 key 的问题**：

```javascript
// 原始列表
[{ id: 1, name: 'A' }, { id: 2, name: 'B' }]

// 在头部插入新项
[{ id: 3, name: 'C' }, { id: 1, name: 'A' }, { id: 2, name: 'B' }]

// 如果用 index 作为 key：
// 旧: [0-A, 1-B]  新: [0-C, 1-A, 2-B]
// Diff 会认为：0 位置 A→C（更新），1 位置 B→A（更新），2 位置新增 B
// 结果：3 次 DOM 操作，且输入框等状态错乱

// 如果用 id 作为 key：
// Diff 识别出：id=3 是新增，id=1 和 id=2 只是位置变化
// 结果：1 次插入 + 2 次移动，输入框状态正确保留
```

**key 的最佳实践**：
- 使用**唯一且稳定**的标识（数据库主键、UUID）
- 避免使用 `index`（除非列表只增不删不改顺序）
- 避免使用 `Math.random()`（每次渲染 key 都变，导致全部重建）

---

**5. Vue 3 的 Diff 优化**

**Vue 2 的问题**：
- 每个组件更新时，需要遍历整个组件的虚拟 DOM 树
- 即使大部分内容是静态的，也要参与 Diff

**Vue 3 的优化策略**：

| 优化手段 | 原理 | 效果 |
|---------|------|------|
| **静态提升（Hoist Static）** | 编译时将静态节点提升到渲染函数外部 | 静态节点只创建一次，跳过 Diff |
| **PatchFlag（动态标记）** | 编译时标记节点的动态部分（文本、class、style 等） | Diff 时只对比标记的部分 |
| **Block Tree** | 将动态节点收集到数组，Diff 只遍历动态节点 | 跳过大量静态节点的比较 |

**PatchFlag 示例**：

```javascript
// 模板
// <div class="static">
//   <p>静态文本</p>
//   <p>{{ dynamicText }}</p>
//   <p :class="activeClass">动态 class</p>
// </div>

// Vue 3 编译后的渲染函数（简化）
function render(_ctx) {
  // 静态节点被提升，只创建一次
  const _hoisted_1 = createStaticVNode("<p>静态文本</p>", 1);
  
  return openBlock(), createElementBlock("div", { class: "static" }, [
    _hoisted_1,  // 静态节点，直接复用
    createElementVNode("p", null, _ctx.dynamicText, 1 /* TEXT */),  // 只对比文本
    createElementVNode("p", {
      class: normalizeClass(_ctx.activeClass)
    }, null, 2 /* CLASS */)  // 只对比 class
  ]);
}
```

**PatchFlag 类型枚举**：
- `1` (TEXT)：动态文本内容
- `2` (CLASS)：动态 class 绑定
- `4` (STYLE)：动态 style 绑定
- `8` (PROPS)：动态非 class/style 属性
- `16` (FULL_PROPS)：有动态 key 属性，需要完整 Diff
- `32` (HYDRATE_EVENTS)：需要客户端注水事件
- `64` (STABLE_FRAGMENT)：子节点顺序不变的 Fragment
- `128` (KEYED_FRAGMENT)：有 key 或部分子节点有 key
- `256` (UNKEYED_FRAGMENT)：无 key 的子节点
- `512` (NEED_PATCH)：组件需要强制更新
- `1024` (DYNAMIC_SLOTS)：动态插槽
- `2048` (DEV_ROOT_FRAGMENT)：开发环境根片段
- `-1` (HOISTED)：已提升的静态节点，跳过 Diff
- `-2` (BAIL)：退出优化模式，采用完整 Diff

---

**6. React 的 Diff 算法差异**

| 特性 | Vue 2 | Vue 3 | React |
|------|-------|-------|-------|
| **对比策略** | 双端比较 | 双端比较 + Block Tree | 单链表遍历（从左到右） |
| **key 作用** | 查找复用节点 | 查找复用节点 + 快速路径 | 查找复用节点 |
| **列表 Diff** | O(n) | O(n)，跳过静态节点 | O(n)，但需要 key |
| **编译优化** | 无 | PatchFlag + 静态提升 | 无（依赖开发者 memo） |
| **子节点对比** | 同时对比头尾 | 同时对比头尾 | 只从左到右对比 |

**React 的 Reconciliation（协调）**：

React 使用**单链表遍历**（而非双端）：
1. 遍历新列表，在旧列表中查找相同 key 的节点
2. 找到则复用并移动，找不到则创建
3. 遍历完成后，旧列表中未匹配的节点删除

React 18 引入了 **Automatic Batching** 和 **Concurrent Rendering**，可以中断和恢复 Diff 过程，提升用户体验。

---

**7. Diff 算法的性能陷阱**

```vue
<!-- 陷阱 1：在 v-for 中使用 index 作为 key -->
<div v-for="(item, index) in list" :key="index">
  <input v-model="item.value">
</div>
<!-- 列表重排时，输入框内容会错乱 -->

<!-- 陷阱 2：key 不稳定（使用随机数） -->
<div v-for="item in list" :key="Math.random()">
<!-- 每次渲染 key 都不同，导致全部销毁重建 -->

<!-- 陷阱 3：深层嵌套对象的过度更新 -->
<div>
  <Child :data="hugeObject" />  <!-- hugeObject 引用没变但内部属性变了 -->
</div>
<!-- Vue 2 检测不到深层变化，React 需要 memo 优化 -->

<!-- 优化方案：拆分组件，精细化控制更新 -->
<div>
  <Child v-for="item in list" :key="item.id" :name="item.name" />
</div>
<!-- 每个子组件独立更新，减少 Diff 范围 -->
```

---

**总结**：Diff 算法是虚拟 DOM 的核心，通过**同层比较**、**双端对比**、**key 复用**三大策略，将树对比的 O(n³) 优化到 O(n)。Vue 3 进一步通过**编译时标记**（PatchFlag）和**静态提升**减少参与 Diff 的节点数量，大幅提升性能。理解 Diff 原理，有助于写出高性能的列表渲染代码，避免 key 使用的常见陷阱。

---

## 四、武汉中地云申

### 15. 单点登录

**单点登录（SSO）**：用户只需登录一次，即可访问多个相互信任的系统。

**常见方案**：

1. **Cookie 方案**（同域名）：
   - 设置 Cookie 的 domain 为父域名（如 `.example.com`）
   - 子域名共享 Cookie

2. **Token 方案**（跨域）：
   - 登录中心颁发 Token，存入 LocalStorage
   - 各系统通过 URL 参数或 postMessage 获取 Token
   - 统一认证服务验证 Token

3. **CAS（Central Authentication Service）**：
   - 独立认证中心（如 Keycloak、Authing）
   - 流程：应用 A 未登录 → 重定向到 CAS → 登录成功返回 Ticket → 应用 A 验证 Ticket → 建立会话
   - 访问应用 B → 重定向到 CAS（已登录）→ 直接返回 Ticket → 应用 B 验证 → 自动登录

---

### 16. vuex

（同上面第7题）

---

### 17. vue 的声明周期有哪些，data 里面的数据初始化是在哪个阶段

**Vue 2 生命周期**：
- `beforeCreate`：实例初始化，数据观测和事件配置之前
- **`created`**：实例创建完成，**data 已初始化**，DOM 未生成
- `beforeMount`：挂载之前，模板编译完成
- `mounted`：挂载完成，DOM 已生成
- `beforeUpdate`：数据更新，DOM 更新之前
- `updated`：DOM 更新完成
- `beforeDestroy`：实例销毁之前
- `destroyed`：实例销毁完成

**Vue 3 生命周期**（选项式 API）：
- `beforeCreate` / `created` → 被 `setup()` 替代
- `beforeMount` → `onBeforeMount`
- `mounted` → `onMounted`
- `beforeUpdate` → `onBeforeUpdate`
- `updated` → `onUpdated`
- `beforeUnmount` → `onBeforeUnmount`
- `unmounted` → `onUnmounted`

**data 初始化时机**：
- 在 `created` 钩子之前完成初始化
- 在 `beforeCreate` 中无法访问 data，在 `created` 中可以访问

---

### 18. vue 双向数据绑定的原理

（同上面第9题）

---

### 19. 闭包

**闭包（Closure）**：函数能够记住并访问它的词法作用域，即使这个函数在词法作用域之外执行。

```javascript
function makeCounter() {
  let count = 0;
  return function() {
    return ++count; // 内部函数访问外部函数的变量
  };
}
const counter = makeCounter();
console.log(counter()); // 1
console.log(counter()); // 2
```

**闭包的作用**：
1. **数据私有化**：模拟私有变量和方法
2. **函数柯里化**：参数复用
3. **防抖节流**：保留定时器引用
4. **循环中保留索引**：`for` 循环中使用 `let` 或闭包固定变量

**闭包的缺点**：
- 内存泄漏风险（引用的变量无法被垃圾回收）
- 解决：及时释放引用（如将变量设为 `null`）

---

### 20. https 为什么比 http 更安全

（同上面第4题）

**补充**：

**HTTPS 的安全机制**：
1. **加密**：使用对称加密传输数据，防止窃听
2. **身份认证**：通过数字证书验证服务器身份，防止冒充
3. **完整性保护**：使用 MAC（消息认证码）防止数据篡改

**SSL/TLS 握手过程**：
1. 客户端发送支持的加密套件列表 + 随机数
2. 服务端返回证书 + 选择的加密套件 + 随机数
3. 客户端验证证书，生成预主密钥（Pre-master secret），用公钥加密发送
4. 双方用随机数 + 预主密钥生成会话密钥（对称密钥）
5. 后续使用对称加密通信

---

### 21. 为什么 data 属性是一个函数而不是一个对象

**Vue 组件中的 data 必须是函数**：

```javascript
// 正确
export default {
  data() {
    return { count: 0 };
  }
};

// 错误（会导致数据共享）
export default {
  data: { count: 0 }
};
```

**原因**：
- Vue 组件可能被多次复用（多个实例）
- 如果 data 是对象，所有实例会共享同一个引用
- 一个实例修改数据，会影响其他实例
- data 作为函数，每次返回新对象，保证各实例数据独立

**根实例例外**：
- `new Vue({ data: { count: 0 } })` 可以是对象，因为根实例只有一个

---

### 22. ts 熟悉吗

TypeScript 是 JavaScript 的超集，添加了静态类型系统。

**核心特性**：
- **类型注解**：`let name: string = 'Tom'`
- **接口（Interface）**：定义对象结构
- **泛型（Generic）**：`function identity<T>(arg: T): T`
- **枚举（Enum）**：`enum Color { Red, Green }`
- **类型推断**：自动推导变量类型
- **联合类型**：`let value: string | number`
- **类型守卫**：`typeof`、`instanceof`、`in` 做类型收窄
- **装饰器（Decorator）**：`@Component`
- **命名空间 / 模块**：组织代码

**优点**：
- 编译时类型检查，减少运行时错误
- 更好的 IDE 支持（智能提示、重构）
- 代码可读性和可维护性提升

---

### 23. axios 封装过没

**封装思路**：

```javascript
// request.js
import axios from 'axios';

const service = axios.create({
  baseURL: process.env.VUE_APP_BASE_API,
  timeout: 5000
});

// 请求拦截器
service.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token');
    if (token) config.headers['Authorization'] = `Bearer ${token}`;
    return config;
  },
  error => Promise.reject(error)
);

// 响应拦截器
service.interceptors.response.use(
  response => {
    const { code, data, message } = response.data;
    if (code === 200) return data;
    // 统一错误处理
    return Promise.reject(new Error(message));
  },
  error => {
    if (error.response?.status === 401) {
      // 登录过期处理
      logout();
    }
    return Promise.reject(error);
  }
);

export default service;
```

**封装内容**：
- 统一 baseURL 和超时时间
- 请求/响应拦截（Token、Loading、错误处理）
- 统一数据格式处理
- 取消请求封装
- 下载/上传文件封装

---

### 24. 封装过哪些组件

**建议按类型回答**：

**UI 组件**：
- 通用表格（支持排序、筛选、分页、多选）
- 表单生成器（根据 JSON Schema 动态渲染表单）
- 文件上传（支持拖拽、进度条、图片预览）
- 富文本编辑器封装

**业务组件**：
- 组织架构树（懒加载、搜索）
- 权限按钮（根据角色/权限控制显示）
- 数据大屏图表（ECharts 封装）
- 地图组件（高德/百度地图封装）

**工具组件**：
- 无限滚动列表（Virtual Scroll）
- 图片懒加载
- 水印组件

**设计思路**：
-  Props / Events / Slots 设计
- 可配置性 vs 约定性
- 文档和示例（Storybook）

---

### 25. vue 的自定义指令用过没

**内置指令**：`v-if`、`v-for`、`v-model`、`v-show`、`v-bind`、`v-on`

**自定义指令**：

```javascript
// Vue 3
const vFocus = {
  mounted(el) {
    el.focus();
  }
};

// 注册
app.directive('focus', vFocus);

// 使用
// <input v-focus />
```

**钩子函数**（Vue 3）：
- `created`：绑定元素的 attribute 或事件监听器被应用之前
- `beforeMount`：指令第一次绑定到元素时
- `mounted`：绑定元素的父组件挂载后
- `beforeUpdate`：更新前
- `updated`：更新后
- `beforeUnmount`：卸载前
- `unmounted`：卸载后

**常用场景**：
- 权限控制（`v-permission`）
- 水印（`v-watermark`）
- 图片懒加载（`v-lazy`）
- 输入框自动聚焦（`v-focus`）
- 防抖（`v-debounce`）

---

## 五、达昌技术

### 26. 图片懒加载和预加载

**懒加载（Lazy Load）**：
- 图片进入可视区域时才加载
- 实现方式：
  1. `loading="lazy"`（原生，Chrome 76+）
  2. `IntersectionObserver` API
  3. 监听 scroll 事件 + getBoundingClientRect

```javascript
// IntersectionObserver 实现
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const img = entry.target;
      img.src = img.dataset.src;
      observer.unobserve(img);
    }
  });
});

document.querySelectorAll('img[data-src]').forEach(img => observer.observe(img));
```

**预加载（Preload）**：
- 提前加载后续可能需要的资源
- 方式：
  1. `<link rel="preload" href="image.jpg" as="image">`
  2. `new Image().src = 'url'`
  3. 骨架屏 + 数据预请求

**区别**：
- 懒加载：延迟加载，减少初始请求，优化首屏
- 预加载：提前加载，优化后续体验

---

### 27. ES6 新增哪些特性

（同上面第6题）

---

### 28. 深拷贝和浅拷贝

（同上面第15题）

---

## 六、百安居一面（React 项目相关）

### 1. 项目中遇到的难点

**建议回答框架**：
- **场景**：如"大屏可视化项目，数据量大，渲染卡顿"
- **分析**：Chrome Performance 分析，发现是 DOM 节点过多导致重排
- **方案**：
  - 引入虚拟滚动（Virtual List），只渲染可视区域 DOM
  - 使用 Canvas 替代 DOM 渲染散点图
  - 数据分片加载，配合 Web Worker 做数据处理
- **结果**：从 5 秒加载优化到 1 秒内，流畅度从 15fps 提升到 60fps

**其他常见难点**：
- 复杂表单校验联动（使用 Formik + Yup）
- 微前端接入 legacy 系统（qiankun）
- 移动端适配（rem/vw + 高清屏处理）

---

### 2. 说说自己做的比较好的项目

（同上面第14题，用 STAR 法则）

---

### 3. react 中有对状态管理做进一步封装吗

**封装方向**：

1. **Hooks 封装**：
```javascript
// useAuth.js
function useAuth() {
  const [user, setUser] = useState(null);
  const login = async (credentials) => { ... };
  const logout = () => { ... };
  return { user, login, logout };
}
```

2. **Context + useReducer**：
- 封装通用状态逻辑，避免 prop drilling

3. **Redux Toolkit**：
- 封装 Slice、Thunk、Selector
- 自动生成 action creators 和 reducer

4. **Zustand / Jotai**：
- 轻量级状态管理，Hooks 风格

---

### 4. react 中在父组件中如何获取子组件的方法

**方案**：

1. **useRef + useImperativeHandle**（推荐）：

```javascript
// 子组件
const Child = forwardRef((props, ref) => {
  const doSomething = () => console.log('child method');
  
  useImperativeHandle(ref, () => ({
    doSomething
  }));
  
  return <div>Child</div>;
});

// 父组件
function Parent() {
  const childRef = useRef();
  const handleClick = () => childRef.current.doSomething();
  return <Child ref={childRef} />;
}
```

2. **状态提升**：
- 将子组件状态和方法提升到父组件，通过 props 控制

**注意**：
- React 推荐数据流自上而下，尽量少用 ref 操作子组件
- 命令式操作破坏数据流的单向性

---

### 5. 低代码和微前端有了解吗？

**低代码（Low-Code）**：
- 通过可视化拖拽和配置生成应用，减少手写代码
- 核心：组件库 + 渲染引擎 + 数据绑定 + 逻辑编排
- 代表产品：OutSystems、钉钉宜搭、腾讯微搭
- 前端实现：JSON Schema 描述页面结构，渲染器解析渲染

**微前端**：
（同上面第6题）

---

### 6. useCallback 使用过没？

**useCallback**：缓存函数引用，避免子组件不必要的重渲染。

```javascript
const memoizedCallback = useCallback(
  () => doSomething(a, b),
  [a, b]
);
```

**使用场景**：
1. 函数作为 prop 传递给子组件，且子组件做了 `React.memo` 优化
2. 函数作为依赖项传给其他 Hook（如 `useEffect`）

**注意**：
- 不是所有函数都需要 useCallback，有开销
- 只有当函数被缓存的依赖项消费时才有价值
- `React.memo` + `useCallback` 常配合使用

**useMemo vs useCallback**：
- `useMemo`：缓存计算结果（值）
- `useCallback`：缓存函数本身

---

### 7. 函数组件和类组件处理重复渲染有什么区别？

**类组件**：
- `shouldComponentUpdate(nextProps, nextState)`：手动控制是否更新
- `PureComponent`：浅比较 props 和 state
- `React.memo`（函数组件）的类组件版本

**函数组件**：
- `React.memo(Component, areEqual)`：类似 PureComponent
- `useMemo`：缓存计算值
- `useCallback`：缓存函数
- 优化思路：保证引用稳定（props、state、context）

**重复渲染原因**：
1. 父组件更新导致子组件更新（即使 props 没变）
2. State 变化（即使值相同，如 `setState({})`）
3. Context 变化（所有消费者重渲染）

---

### 8. 封装的按钮权限组件怎么实现的？

**实现方案**：

```javascript
// AuthButton.jsx
function AuthButton({ permission, children, ...rest }) {
  const { userPermissions } = useAuth();
  
  if (!userPermissions.includes(permission)) {
    return null; // 或返回禁用状态的按钮
  }
  
  return <Button {...rest}>{children}</Button>;
}

// 使用
// <AuthButton permission="user:create">创建用户</AuthButton>
```

**进阶**：
- 支持多个权限（`permissions` 数组，配置逻辑"与"/"或"）
- 权限指令（`v-permission`）封装
- 后端返回权限码，前端做映射
- 路由级别权限 + 按钮级别权限

---

### 9. 数据什么时候定义在组件里面，什么时候定义在状态管理里面？

| 定义位置 | 适用场景 |
|---------|---------|
| 组件 State | 局部状态，仅当前组件使用（如表单输入、弹窗显隐） |
| 状态管理 | 全局共享状态，多个组件需要访问（如用户信息、主题、购物车） |

**判断标准**：
1. **作用范围**：只有当前组件用 → State；跨组件共享 → Store
2. **持久化需求**：需要持久化或恢复 → Store
3. **数据复杂度**：简单状态 → State；复杂关联数据 → Store
4. **服务端数据**：从 API 获取且多处使用 → Store（配合缓存策略）

---

### 10. 方法什么时候写在父组件中，什么时候写在子组件中？

| 位置 | 场景 |
|------|------|
| 父组件 | 涉及多个子组件协作、操作父组件状态、需要状态提升时 |
| 子组件 | 仅操作子组件内部状态、封装可复用逻辑、组件自包含时 |

**原则**：
- **单一职责**：子组件只处理自己的逻辑
- **状态就近**：数据在哪里使用，逻辑就放在哪里
- **复用性**：可复用的逻辑封装在子组件或自定义 Hook 中
- **性能**：频繁更新的状态尽量下沉，减少不必要的父组件渲染

---

## 七、万云科技

### 1. react 用过哪些 hooks？

**基础 Hooks**：
- `useState`：状态管理
- `useEffect`：副作用（数据获取、订阅、手动 DOM 操作）
- `useContext`：消费 Context

**性能优化 Hooks**：
- `useMemo`：缓存计算值
- `useCallback`：缓存函数
- `React.memo`：组件级别的记忆化

**Ref Hooks**：
- `useRef`：获取 DOM 引用、保存可变值（不触发重渲染）
- `useImperativeHandle`：配合 forwardRef 暴露子组件方法

**其他 Hooks**：
- `useReducer`：复杂状态逻辑，替代 useState
- `useLayoutEffect`：同步执行副作用，在浏览器绘制之前
- `useId`（React 18）：生成唯一 ID，用于无障碍属性
- `useTransition`（React 18）：标记非紧急更新，避免阻塞 UI
- `useDeferredValue`（React 18）：延迟更新非紧急部分
- `useSyncExternalStore`：订阅外部状态（如 Redux）
- `useInsertionEffect`：CSS-in-JS 库插入样式的时机

**自定义 Hooks**：
- `useFetch`、`useLocalStorage`、`useDebounce`、`usePrevious`

---

### 2. 数组合并有哪些方法

```javascript
const arr1 = [1, 2];
const arr2 = [3, 4];

// 1. concat（返回新数组，不改变原数组）
const result = arr1.concat(arr2); // [1, 2, 3, 4]

// 2. 展开运算符（ES6）
const result = [...arr1, ...arr2];

// 3. push + 展开（改变原数组）
arr1.push(...arr2);

// 4. Array.prototype.push.apply
Array.prototype.push.apply(arr1, arr2);

// 5. 遍历 push（改变原数组）
arr2.forEach(item => arr1.push(item));

// 6. unshift（头部合并，改变原数组）
arr2.unshift(...arr1);
```

---

### 3. 请求参数如何防篡改

**方案**：

1. **签名机制（Signature）**：
   - 前端将参数按 key 排序 + 时间戳 + 密钥生成签名
   - 后端用同样算法验证签名
   - 示例：
   ```javascript
   const sign = md5(`${sortedParams}&timestamp=${ts}&key=SECRET_KEY`);
   ```

2. **HTTPS**：
   - 防止传输过程中被中间人篡改

3. **Token 验证**：
   - JWT 或 Session Token，后端校验请求合法性

4. **参数加密**：
   - 敏感字段用 AES/RSA 加密

5. **防重放攻击**：
   - 加入时间戳和随机数（nonce），后端校验时间窗口和 nonce 唯一性

---

### 4. localStorage 如何跨域获取

**同源策略限制**：localStorage 遵循同源策略，不同域名无法直接访问。

**跨域方案**：

1. **postMessage**（同一浏览器，不同窗口）：
```javascript
// A 页面（a.com）
window.parent.postMessage(data, 'https://b.com');

// B 页面（b.com）
window.addEventListener('message', (e) => {
  if (e.origin === 'https://a.com') {
    localStorage.setItem('key', e.data);
  }
});
```

2. **服务端中转**：
   - 各域将数据同步到同一服务端，需要时拉取

3. **统一域名（反向代理）**：
   - 不同子域通过 Nginx 代理到同一域下

4. **Cookie（设置 domain）**：
   - 子域可共享父域 Cookie，但 localStorage 不支持 domain 设置

---

### 5. 如何写一个 split 方法并覆盖数组的原方法

**实现思路**：

```javascript
// 保存原方法
const originalSplit = Array.prototype.split;

// 覆盖
Array.prototype.split = function(separator, limit) {
  console.log('自定义 split 被调用');
  
  // 调用原方法或自定义逻辑
  if (originalSplit) {
    return originalSplit.call(this, separator, limit);
  }
  
  // 如果原方法不存在，自行实现
  const result = [];
  let str = String(this);
  // ... 具体实现
  return result;
};

// 更好的方式：不要直接修改原生原型！
// 推荐：使用 Symbol 或自定义类
```

**警告**：
- 修改原生原型是**反模式**，可能导致：
  1. 与其他库冲突
  2. 未来 JS 版本新增同名方法导致覆盖失效
  3. 难以调试和维护
- 如果必须扩展，使用 `Object.defineProperty` 设置为不可枚举

---

### 6. forEach 循环和 for 循环哪个性能高？forEach 循环可以中断吗？

**性能对比**：

| 循环方式 | 性能 | 特点 |
|---------|------|------|
| `for` | 最快 | 最基础，无函数调用开销 |
| `for...of` | 较快 | 可迭代对象，有迭代器开销 |
| `forEach` | 较慢 | 函数回调，每次迭代都是函数调用 |
| `map/filter` | 较慢 | 创建新数组，有额外开销 |

**结论**：普通 `for` 循环性能最好，但差距在大多数场景可忽略。

**forEach 能否中断**：
- **不能**使用 `break`、`continue`、`return` 中断整个循环
- 只能使用 `return` 跳过当前迭代（类似 `continue`）
- 想中断需抛异常（不推荐）：
```javascript
try {
  arr.forEach(item => {
    if (item === target) throw new Error('break');
  });
} catch (e) {}
```

**可中断的替代方案**：
- `for` / `for...of` + `break`
- `some()` / `every()`：返回 `true`/`false` 可中断

---

### 7. 前端性能优化？

（同上面第10题）

---

### 8. 做的最好和最有成就感的项目是哪个

**回答建议**：
- 选择有技术挑战、可量化成果的项目
- 突出：技术难度、团队协作、业务价值
- 示例：
  
  > "最有成就感的是一个电商数据大屏项目。技术上用 WebGL 替代 Canvas 渲染 10 万+ 数据点，实现 60fps 流畅交互；业务上帮助运营团队实时监控 GMV，双 11 期间零故障。项目还获得了公司年度技术创新奖。"

---

### 9. 性格优缺点、爱好、前同事评价、未来规划

**性格优缺点**：
- 优点：细致（适合前端）、学习能力强、沟通能力好
- 缺点：完美主义（有时过度纠结细节，正在改进）

**爱好**：
- 技术：逛 GitHub、写技术博客、参与开源
- 生活：运动（体现抗压能力）、摄影（审美能力，适合前端）

**前同事评价**：
- 技术扎实、靠谱、团队协作好
- 举例："上次项目紧急上线，主动承担核心模块，加班配合测试"

**未来规划**：
- 短期（1-2年）：深入 React/Vue 源码，提升架构能力
- 中期（3-5年）：成长为前端专家或技术负责人
- 长期：关注前端工程化、性能优化、可视化等方向

---

## 附录：高频知识点速查

### 防抖 vs 节流
- 防抖：事件停止后执行（搜索框）
- 节流：间隔执行（滚动加载）

### 宏任务 vs 微任务
- 宏：setTimeout、setInterval、I/O、UI render
- 微：Promise.then、MutationObserver、queueMicrotask

### HTTP 缓存
- 强缓存：Cache-Control、Expires
- 协商缓存：Last-Modified/ETag

### Vue2 vs Vue3 响应式
- Vue2：Object.defineProperty
- Vue3：Proxy

### 跨域方案
- CORS、JSONP、代理、postMessage、WebSocket

### 深拷贝方案
- JSON.parse/stringify（简单场景）
- 递归 + WeakMap（处理循环引用）
- lodash.cloneDeep（生产环境）

### React 性能优化
- React.memo、useMemo、useCallback
- 状态下沉、代码分割、懒加载

---

*本文档由面试题目图片整理生成，涵盖了 2024-2025 年武汉地区前端面试高频考点。*